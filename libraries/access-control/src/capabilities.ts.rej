diff a/libraries/access-control/src/capabilities.ts b/libraries/access-control/src/capabilities.ts	(rejected hunks)
@@ -1,14 +1,9 @@
-import { MediaServerAspect, MediaServerCapability } from "./contexts/media-server"
-
 /**
  * A capability is a single string that combines an aspect and an action,
  * separating them with a dot.
  * 
  *    {Aspect}.{Action}
  * 
- * The aspect must be defined in the Aspects array, and the action can be any
- * arbitrary string except an asterisk (*).
- * 
  * Capability Assignments are stored as an array of capability strings. The
  * strings can be exact matching capabilities, or can use of a wildcard for
  * selecting broader ranges of capabilities.
@@ -26,17 +21,23 @@ import { MediaServerAspect, MediaServerCapability } from "./contexts/media-serve
  *    aspect.
  */
 export type CapabilityAssignment =
-  MediaServerCapability
   | '*.*'
-  | `${MediaServerAspect}.*`
+  | `${string}.*`
   | `*.${string}`
+  | `${string}.${string}`
 
 export type CapabilityAssignments = CapabilityAssignment[]
 
 /**
- * Validate the syntax of an capability assignment.
+ * Validate the syntax of an capability assignment. This will check:
+ * 
+ *    - That it's a string
+ *    - That it has exactly 1 dot
+ *    - That the left and right sides of the dot are not empty
+ * 
+ * This does *not* check that the capability exists in the master list.
  */
-const validateCapabilitySyntax = (assignment: CapabilityAssignment): boolean => {
+const validateCapabilitySyntax = (assignment: string): boolean => {
   if (typeof assignment !== 'string') {
     return false
   }
@@ -62,7 +63,8 @@ const validateCapabilitySyntax = (assignment: CapabilityAssignment): boolean =>
 }
 
 /**
- * Validate the syntax of an array of capability assignments.
+ * Validate the syntax of an array of capability assignments. All array items
+ * must be valid.
  */
 const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments = []): boolean => {
   let allAreValid = true
@@ -78,10 +80,11 @@ const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments = []):
 
 /**
  * Checks whether the required capability is satisfied by any of the given
- * capability assignments.
+ * capability assignments. Use the generic for autocomplete.
  */
-export function hasCapability(
-  requiredCapability: MediaServerCapability,
+export function hasCapability<TCapabilityContext extends string>(
+  requiredCapability: TCapabilityContext,
+  // FIXME do I need this type? It has all caps from all contexts
   assignments: CapabilityAssignments = [],
 ) {
   if (!validateCapabilitySyntax(requiredCapability)) {
@@ -129,8 +132,8 @@ export function hasCapability(
  * Like `hasCapability()` but for more than 1 capability. Returns true if ALL
  * capabilities are satisfied.
  */
-export function hasCapabilities(
-  requiredCapabilities: MediaServerCapability[],
+export function hasCapabilities<TCapabilityContext extends string>(
+  requiredCapabilities: TCapabilityContext[],
   assignments: CapabilityAssignments = [],
 ) {
   if (!requiredCapabilities) {
@@ -143,7 +146,7 @@ export function hasCapabilities(
 
   const allCapabilitiesAreSatisfied = requiredCapabilities.every((requiredCapability) => {
     const satisfiedByRole = assignments?.find((assignment: CapabilityAssignment) => {
-      return !!hasCapability(requiredCapability, [assignment])
+      return !!hasCapability<TCapabilityContext>(requiredCapability, [assignment])
     })
     return !!satisfiedByRole
   })
