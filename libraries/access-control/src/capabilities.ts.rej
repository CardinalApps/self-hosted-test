diff a/libraries/access-control/src/capabilities.ts b/libraries/access-control/src/capabilities.ts	(rejected hunks)
@@ -67,7 +67,7 @@ const validateCapabilitySyntax = <TCapabilityContext>(assignment: CapabilityAssi
  * Validate the syntax of an array of capability assignments. All array items
  * must be valid.
  */
-const validateCapabilitiesSyntax = <TCapabilityContext>(capabilityList: CapabilityAssignments<TCapabilityContext> = []): boolean => {
+const validateCapabilitiesSyntax = <Caps>(capabilityList: CapabilityAssignments<Caps> = []): boolean => {
   let allAreValid = true
 
   for (const capability of capabilityList) {
@@ -83,10 +83,14 @@ const validateCapabilitiesSyntax = <TCapabilityContext>(capabilityList: Capabili
  * Checks whether the required capability is satisfied by any of the given
  * capability assignments. Use the generic for autocomplete.
  */
-export function hasCapability<TCapabilityContext extends string>(
-  requiredCapability: TCapabilityContext,
-  assignments: CapabilityAssignments<TCapabilityContext> = [],
+export function hasCapability<Caps>(
+  requiredCapability: Caps,
+  assignments: CapabilityAssignments<Caps> = [],
 ) {
+  if (typeof requiredCapability !== 'string') {
+    return false
+  }
+
   if (!validateCapabilitySyntax(requiredCapability)) {
     return false
   }
@@ -102,6 +106,10 @@ export function hasCapability<TCapabilityContext extends string>(
   const [requiredAspect, requiredAction] = requiredCapability.split('.')
 
   for (const assignment of assignments) {
+    if (typeof assignment !== 'string') {
+      return false
+    }
+
     const [assignmentAspect, assignmentAction] = assignment.split('.')
     let aspectIsValid = false
     let actionIsValid = false
@@ -132,9 +140,9 @@ export function hasCapability<TCapabilityContext extends string>(
  * Like `hasCapability()` but for more than 1 capability. Returns true if ALL
  * capabilities are satisfied.
  */
-export function hasCapabilities<TCapabilityContext extends string>(
-  requiredCapabilities: TCapabilityContext[],
-  assignments: CapabilityAssignments<TCapabilityContext> = [],
+export function hasCapabilities<Caps>(
+  requiredCapabilities: Caps[],
+  assignments: CapabilityAssignments<Caps> = [],
 ) {
   if (!requiredCapabilities) {
     return true
@@ -145,8 +153,8 @@ export function hasCapabilities<TCapabilityContext extends string>(
   }
 
   const allCapabilitiesAreSatisfied = requiredCapabilities.every((requiredCapability) => {
-    const satisfiedByRole = assignments?.find((assignment: CapabilityAssignment<TCapabilityContext>) => {
-      return !!hasCapability<TCapabilityContext>(requiredCapability, [assignment])
+    const satisfiedByRole = assignments?.find((assignment: CapabilityAssignment<Caps>) => {
+      return !!hasCapability<Caps>(requiredCapability, [assignment])
     })
     return !!satisfiedByRole
   })
