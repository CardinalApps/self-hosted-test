diff a/libraries/access-control/src/capabilities.ts b/libraries/access-control/src/capabilities.ts	(rejected hunks)
@@ -1,12 +1,10 @@
 /**
- * Master list of all components.
+ * Master list of all aspects.
  * 
- * In the context of Access Control, "component" is used as a generic term for
- * anything that can have its access restricted. High level concepts like
- * Resources, Features, Applications, and Services are all categorized as
- * components within the Access Control layer.
+ * In the context of Access Control, an aspect is anything that can have its
+ * access restricted.
  */
-export const Components = [
+export const Aspects = [
   'AdminApp',
   'MusicApp',
   'MusicTracks',
@@ -17,18 +15,18 @@ export const Components = [
   'Users',
 ] as const
 
-export type ComponentNames = typeof Components[number]
+export type AspectNames = typeof Aspects[number]
 
 /**
  * Master list of all capabilities.
  * 
- * A capability is a single string that combines a Component and an Action,
+ * A capability is a single string that combines an aspect and an action,
  * separating them with a dot.
  * 
- *    {Component}.{Action}
+ *    {Aspect}.{Action}
  * 
- * The Component must be defined in the Components type, and the Action can be
- * any arbitrary string except an asterisk (*).
+ * The aspect must be defined in the Aspects array, and the action can be any
+ * arbitrary string except an asterisk (*).
  */
 export const Capabilities = [
   /**
@@ -73,22 +71,22 @@ export type NamedCapability = typeof Capabilities[number]
  * strings can be exact matching capabilities, or can use of a wildcard for
  * selecting broader ranges of capabilities.
  * 
- *    `*.*` - Matches all components and all actions, granting all current and
+ *    `*.*` - Matches all aspects and all actions, granting all current and
  *    future capabilities.
  * 
- *    `MusicApp.*` - Matches all actions in the MusicApp component, granting all
+ *    `MusicApp.*` - Matches all actions in the MusicApp aspect, granting all
  *    current and future MusicApp capabilities.
  * 
  *    `*.Login` - Matches only the Login action on all current and future
- *    components.
+ *    aspects.
  * 
  *    `CinemaApp.Login` - Matches only the Login action on the CinemaApp
- *    component.
+ *    aspect.
  */
 export type CapabilityAssignment =
   NamedCapability
   | '*.*'
-  | `${ComponentNames}.*`
+  | `${AspectNames}.*`
   | `*.${string}`
 
 export type CapabilityAssignments = CapabilityAssignment[]
@@ -156,18 +154,18 @@ export function hasCapability(
     return false
   }
 
-  const [requiredComponent, requiredAction] = requiredCapability.split('.')
+  const [requiredAspect, requiredAction] = requiredCapability.split('.')
 
   for (const assignment of assignments) {
-    const [assignmentComponent, assignmentAction] = assignment.split('.')
-    let componetIsValid = false
+    const [assignmentAspect, assignmentAction] = assignment.split('.')
+    let aspectIsValid = false
     let actionIsValid = false
 
     if (
-      requiredComponent === assignmentComponent
-      || assignmentComponent === '*'
+      requiredAspect === assignmentAspect
+      || assignmentAspect === '*'
     ) {
-      componetIsValid = true
+      aspectIsValid = true
     }
 
     if (
@@ -177,7 +175,7 @@ export function hasCapability(
       actionIsValid = true
     }
 
-    if (componetIsValid && actionIsValid) {
+    if (aspectIsValid && actionIsValid) {
       return true
     }
   }
