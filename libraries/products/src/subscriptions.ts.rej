diff a/libraries/products/src/subscriptions.ts b/libraries/products/src/subscriptions.ts	(rejected hunks)
@@ -1,4 +1,4 @@
-export enum SubscriptionTiers {
+export enum SubscriptionTierSlug {
   FREE = 'free',
   EARLY_ADOPTER = 'early_adopter',
 }
@@ -18,6 +18,9 @@ export type SubscriptionTier = {
   },
 }
 
+/**
+ * All subscriptions with their final values.
+ */
 export const SUBSCRIPTIONS = {
   free: {
     name: {
@@ -25,7 +28,7 @@ export const SUBSCRIPTIONS = {
     },
     slug: "free",
     provides: {
-      seats: 2,
+      seats: 5,
     },
   },
   early_adopter: {
@@ -37,6 +40,17 @@ export const SUBSCRIPTIONS = {
       seats: 10,
     },
   },
-} as Record<SubscriptionTiers, SubscriptionTier>
+} as Record<SubscriptionTierSlug, SubscriptionTier>
+
+/**
+ * Primary API for getting a subscription.
+ */
+export const getSubscription = (slug: string): SubscriptionTier | null => {
+  if (slug in SUBSCRIPTIONS) {
+    return SUBSCRIPTIONS[slug as SubscriptionTierSlug]
+  } else {
+    return null
+  }
+}
 
 export default SUBSCRIPTIONS
