diff a/libraries/ui/public/styles/global.css b/libraries/ui/public/styles/global.css	(rejected hunks)
@@ -58,3 +58,15 @@
   color: var(--danger);
   i { color: var(--danger); }
 }
+
+@keyframes inniePop {
+  0% {
+    transform: scale(100%);
+  }
+  10% {
+    transform: scale(97%);
+  }
+  100% {
+    transform: scale(100%);
+  }
+}
