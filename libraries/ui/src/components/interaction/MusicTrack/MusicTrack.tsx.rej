diff a/libraries/ui/src/components/interaction/MusicTrack/MusicTrack.tsx b/libraries/ui/src/components/interaction/MusicTrack/MusicTrack.tsx	(rejected hunks)
@@ -1,18 +1,20 @@
 import { useContext, type PropsWithChildren } from 'react'
-import { useDispatch, useSelector } from 'react-redux'
+import { useSelector } from 'react-redux'
 
 import Icon from '../../typography/Icon'
 import MusicPlaybackButton from '../MusicPlaybackButton'
 
 //import { settingsSelectors } from '../../../store/slices/settings'
-import { musicActions, musicSelectors } from '../../../store/slices/music'
+import { musicSelectors } from '../../../store/slices/music'
+import playMusic from '../../../store/slices/music/thunks/play'
 import { RouterContext } from '../../../context/router'
 
 import './MusicTrack.css'
 import { useReleaseCover } from '../../../hooks/useReleaseCover'
+import { useAppDispatch } from '../../../hooks/useAppDispatch'
 
 type MusicTrackProps = {
-  musicTrackId?: number,
+  musicTrackId?: string,
   trackNumber?: number,
   trackTitle?: string,
   releaseTitle?: string,
@@ -38,7 +40,7 @@ const MusicTrack = ({
   plays,
   hasArtwork = true,
 }: PropsWithChildren<MusicTrackProps>) => {
-  const dispatch = useDispatch()
+  const dispatch = useAppDispatch()
   const { Link } = useContext(RouterContext)
   //const { lang } = useSelector(settingsSelectors.current)
   const playing = useSelector(musicSelectors.playing)
@@ -52,7 +54,9 @@ const MusicTrack = ({
       && !e.target.matches('.music-playback-button')
       && !e.target.closest('.music-playback-button')
     ) {
-      dispatch(musicActions.play({ trackId: musicTrackId }))
+      dispatch(playMusic({
+        trackId: musicTrackId,
+      }))
     }
   }
 
