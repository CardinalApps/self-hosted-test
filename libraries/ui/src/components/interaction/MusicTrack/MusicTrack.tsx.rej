diff a/libraries/ui/src/components/interaction/MusicTrack/MusicTrack.tsx b/libraries/ui/src/components/interaction/MusicTrack/MusicTrack.tsx	(rejected hunks)
@@ -25,6 +25,7 @@ type MusicTrackProps = {
   duration?: string,
   plays?: number,
   hasArtwork?: boolean,
+  canFav?: boolean,
 }
 
 const MusicTrack = ({
@@ -39,6 +40,7 @@ const MusicTrack = ({
   duration,
   plays,
   hasArtwork = true,
+  canFav = true,
 }: PropsWithChildren<MusicTrackProps>) => {
   const dispatch = useAppDispatch()
   const { Link } = useContext(RouterContext)
@@ -91,9 +93,11 @@ const MusicTrack = ({
       </div>
       {!!duration && <p className="col music-track-duration">{duration}</p>}
       {!!plays && <p className="col music-track-plays"><span>{plays}</span></p>}
-      <div className="col music-track-favorite">
-        <Icon fa="fas fa-star" hoverType="icon" onClick={() => console.log('fav')} />
-      </div>
+      {!!canFav &&
+        <div className="col music-track-favorite">
+          <Icon fa="fas fa-star" hoverType="icon" onClick={() => console.log('fav')} />
+        </div>
+      }
       {!!artwork &&
         <div className="col music-track-artwork">
           <img src={artwork} />
