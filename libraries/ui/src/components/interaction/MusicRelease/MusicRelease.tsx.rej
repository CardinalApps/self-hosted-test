diff a/libraries/ui/src/components/interaction/MusicRelease/MusicRelease.tsx b/libraries/ui/src/components/interaction/MusicRelease/MusicRelease.tsx	(rejected hunks)
@@ -88,29 +88,31 @@ const MusicRelease = ({
           </div> */}
         </div>
       </div>
-      <div className="meta">
-        <div className="row">
-          {!!releaseTitle && (
-            <p className="release-name">
-              {releaseLink && releaseTitle && Link
-                ? <Link to={releaseLink}>{releaseTitle}</Link>
-                : releaseTitle
-              }
-            </p>
-          )}
-        </div>
-        <div className="row">
-          {!!artistName && (
-            <p className="artist-name">
-              {artistLink && artistName && Link
-                ? <Link to={artistLink}>{artistName}</Link>
-                : artistName
-              }
-            </p>
-          )}
-          {!!releaseYear && <p className="release-year">{releaseYear}</p>}
+      {(releaseTitle || artistName || releaseYear) && (
+        <div className="meta">
+          <div className="row">
+            {!!releaseTitle && (
+              <p className="release-name">
+                {releaseLink && releaseTitle && Link
+                  ? <Link to={releaseLink}>{releaseTitle}</Link>
+                  : releaseTitle
+                }
+              </p>
+            )}
+          </div>
+          <div className="row">
+            {!!artistName && (
+              <p className="artist-name">
+                {artistLink && artistName && Link
+                  ? <Link to={artistLink}>{artistName}</Link>
+                  : artistName
+                }
+              </p>
+            )}
+            {!!releaseYear && <p className="release-year">{releaseYear}</p>}
+          </div>
         </div>
-      </div>
+      )}
     </div>
   )
 }
