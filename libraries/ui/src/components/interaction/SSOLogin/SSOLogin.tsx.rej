diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx	(rejected hunks)
@@ -49,9 +49,9 @@ const SSOLogin = ({
   style,
   oidc,
 }: PropsWithChildren<SSOLoginProps>) => {
-  const popupWindow = useRef(null)
-  const popupWindowClosedInterval = useRef(null)
-  const [popupWindowIsOpen, setPopupWindowIsOpen] = useState(false)
+  const ssoFlowWindow = useRef(null)
+  const ssoWindowClosedInterval = useRef(null)
+  const [ssoFlowIsOpen, setSSOFlowIsOpen] = useState(false)
   const [oidcState] = useState(uuid())
 
   const { enable_oidc_beta } = useAppSelector(settingsSelectors.current)
@@ -65,9 +65,13 @@ const SSOLogin = ({
   const handlePopupWindowMessage = (e) => {
     if (debug) console.log('Received postMessage in SSOLogin', e)
 
+    if (!e.isTrusted) {
+      throw new Error('Got untrusted message')
+    }
+
     // Popup says it's ready - send our app ID and permissions
     if (e.origin === ssoUrl && e?.data?.CARDINAL_SSO_READY) {
-      popupWindow.current.postMessage({
+      ssoFlowWindow.current.postMessage({
         appId: appId,
         requestedPermissions: permissions,
         userAgentString: navigator.userAgent,
@@ -97,21 +101,21 @@ const SSOLogin = ({
    * anything until we get a "ready" message from the popup.
    */
   useEffect(() => {
-    if (popupWindowIsOpen) {
+    if (ssoFlowIsOpen) {
       if (typeof onPopupOpened === 'function') {
         onPopupOpened()
       }
 
       window.addEventListener('message', handlePopupWindowMessage)
 
-      if (!popupWindowClosedInterval.current) {
+      if (!ssoWindowClosedInterval.current) {
         // Check every 500ms if the popup is still open.
-        popupWindowClosedInterval.current = setInterval(() => {
-          if (popupWindow.current.closed) {
-            clearInterval(popupWindowClosedInterval.current)
-            popupWindowClosedInterval.current = null
-            popupWindow.current = null
-            setPopupWindowIsOpen(false)
+        ssoWindowClosedInterval.current = setInterval(() => {
+          if (ssoFlowWindow.current.closed) {
+            clearInterval(ssoWindowClosedInterval.current)
+            ssoWindowClosedInterval.current = null
+            ssoFlowWindow.current = null
+            setSSOFlowIsOpen(false)
             if (typeof onPopupClosed === 'function') {
               onPopupClosed()
             }
@@ -121,7 +125,7 @@ const SSOLogin = ({
 
       return () => window.removeEventListener('message', handlePopupWindowMessage)
     }
-  }, [popupWindowIsOpen])
+  }, [ssoFlowIsOpen])
 
 
   const getPopupUrl = () => {
@@ -132,12 +136,12 @@ const SSOLogin = ({
 
     const oidcUrl = new URL(`${CLOUD_AUTH_HOST}/oidc/auth`)
     oidcUrl.searchParams.append('response_type', 'code')
-    oidcUrl.searchParams.append('scope', 'openid')
-    // oidcUrl.searchParams.append('state', oidcState)
-    // oidcUrl.searchParams.append('nonce', uuid())
-    //oidcUrl.searchParams.append('redirect_uri', `${ACCOUNT_APP_HOST}/oidc`) // real
-    oidcUrl.searchParams.append('redirect_uri', `${CLOUD_AUTH_HOST}/oidc`) // dev
     oidcUrl.searchParams.append('client_id', oidc.client_id)
+    oidcUrl.searchParams.append('scope', 'openid')
+    oidcUrl.searchParams.append('state', oidcState)
+    oidcUrl.searchParams.append('nonce', uuid())
+    oidcUrl.searchParams.append('redirect_uri', `${ACCOUNT_APP_HOST}/oidc`)
+    //oidcUrl.searchParams.append('redirect_uri', `${CLOUD_AUTH_HOST}/oidc`) // oidc-provider dev mode
 
     return oidcUrl.href
   }
@@ -150,17 +154,21 @@ const SSOLogin = ({
         rel="noreferrer"
         style={style}
         onClick={() => {
-          if (popupWindow.current) {
-            popupWindow.current.focus()
+          if (ssoFlowWindow.current) {
+            ssoFlowWindow.current.focus()
           } else {
-            // const popupWidth = 525
-            // const popupHeight = 670
-            // const popupLeft = (window.outerWidth / 2) - (popupWidth / 2)
-            // const popupright = (window.outerHeight / 2) - (popupHeight / 2)
-            //const popup = window.open(getPopupUrl(), '_blank', `popup=true, width=${popupWidth}, height=${popupHeight}, left=${popupLeft}, top=${popupright}`)
-            const popup = window.open(getPopupUrl(), '_blank', `popup=false`)
-            popupWindow.current = popup
-            //setPopupWindowIsOpen(true)
+            // TODO maybe make this a user option?
+            const openInPopup = false
+            const popupWidth = 525
+            const popupHeight = 670
+            const popupLeft = (window.outerWidth / 2) - (popupWidth / 2)
+            const popupright = (window.outerHeight / 2) - (popupHeight / 2)
+
+            ssoFlowWindow.current = openInPopup
+              ? window.open(getPopupUrl(), '_blank', `popup=true, width=${popupWidth}, height=${popupHeight}, left=${popupLeft}, top=${popupright}`)
+              : window.open(getPopupUrl())
+
+            setSSOFlowIsOpen(true)
           }
         }}
       >
