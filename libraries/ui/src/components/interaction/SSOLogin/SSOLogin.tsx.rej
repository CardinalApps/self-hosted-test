diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx	(rejected hunks)
@@ -44,7 +44,7 @@ const SSOLogin = ({
   onPopupClosed,
   label = 'Sign in with Cardinal Cloud',
   ssoUrl,
-  debug = false,
+  debug = true,
   enableNewSSOFlow = false,
   style,
 }: PropsWithChildren<SSOLoginProps>) => {
@@ -89,14 +89,15 @@ const SSOLogin = ({
 
     // Popup says it's ready - send our app ID and permissions
     if (e.origin === ssoUrl && e?.data?.CARDINAL_SSO_READY) {
-      ssoFlowWindow.current.postMessage({
+      const dataToSend = {
         appId,
         nonce,
         serverName: server_name,
         requestedPermissions: permissions,
         userAgentString: navigator.userAgent,
-      }, ssoUrl)
-      if (debug) console.log('Sent initial postMessage', e)
+      }
+      ssoFlowWindow.current.postMessage(dataToSend, ssoUrl)
+      if (debug) console.log('Sent initial postMessage', dataToSend)
     }
 
     // Popup got the app ID - handshake complete
