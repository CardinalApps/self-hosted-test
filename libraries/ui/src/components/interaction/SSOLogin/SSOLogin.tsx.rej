diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx	(rejected hunks)
@@ -53,7 +53,7 @@ type SSOLoginProps = {
   onPopupClosed?: () => void,
   label?: string,
   ssoUrl?: string,
-  enableNewSSOFlow?: boolean,
+  enableNewSSOFlow?: boolean | undefined,
   overrideServerName?: string,
   tokenType?: TokenType,
   style?: CSSProperties,
@@ -73,7 +73,7 @@ const SSOLogin = ({
   onPopupClosed,
   label = 'Sign in with Cardinal Cloud',
   ssoUrl,
-  enableNewSSOFlow = false,
+  enableNewSSOFlow = true,
   overrideServerName,
   tokenType = 'JWT',
   style,
@@ -267,10 +267,10 @@ const SSOLogin = ({
         rel="noreferrer"
         style={style}
         onClick={() => {
-          if (enableNewSSOFlow) {
-            handleSSOClick()
-          } else {
+          if (enableNewSSOFlow === false) {
             handleLegacySSOClick()
+          } else {
+            handleSSOClick()
           }
         }}
       >
