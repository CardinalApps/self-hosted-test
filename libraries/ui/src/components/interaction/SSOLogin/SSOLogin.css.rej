diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.css b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.css	(rejected hunks)
@@ -7,6 +7,8 @@
   cursor: pointer;
   display: inline-flex;
   justify-content: center;
+  position: relative;
+  overflow: hidden;
   align-items: center;
   color: var(--text-color-2);
   background-color: var(--bg-2);
@@ -19,6 +21,31 @@
     background-color: var(--bg-3);
   }
 
+  &.loading {
+
+    .loading-indicator {
+      
+      svg {
+        transform: translate(-50%, -50%);
+      }
+    }
+
+    span {
+      transform: translateY(300%)
+    }
+  }
+
+  .loading-indicator {
+    
+    svg {
+      position: absolute;
+      top: 50%;
+      left: 50%;
+      transform: translate(-50%, -300%);
+      transition: transform 200ms;
+    }
+  }
+
   svg {
     margin-right: 8px;
   }
@@ -27,5 +54,6 @@
     font-family: var(--font-family);
     font-weight: 600;
     font-size: 14px;
+    transition: transform 200ms;
   }
 }
