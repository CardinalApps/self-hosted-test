diff a/libraries/ui/src/components/interaction/Carousel/Carousel.tsx b/libraries/ui/src/components/interaction/Carousel/Carousel.tsx	(rejected hunks)
@@ -1,37 +1,88 @@
 import type { PropsWithChildren } from 'react'
 import useEmblaCarousel from 'embla-carousel-react'
+import Button from '../Button'
 
 import './Carousel.css'
 
 type CarouselProps = {
-  initialSlide: number,
+  width?: string | number,
+  height?: string | number,
+  title?: string,
+  prev?: boolean,
+  next?: boolean,
+  initialSlide?: number,
+  items: React.ReactNode[],
 }
 
 /**
  * Run of the mill pagination.
  */
 const Carousel = ({
-  initialSlide,
+  width = '100%',
+  height,
+  title,
+  prev,
+  next,
+  initialSlide = 1,
+  items = [],
 }: PropsWithChildren<CarouselProps>) => {
-  const [emblaRef, emblaApi] = useEmblaCarousel({ loop: false })
-
-  console.log(initialSlide)
+  const [emblaRef, emblaApi] = useEmblaCarousel({
+    loop: false,
+    startIndex: initialSlide - 1,
+  })
 
   const goToPrev = () => emblaApi?.scrollPrev()
   const goToNext = () => emblaApi?.scrollNext()
 
   return (
-    <div className="embla">
-      <div className="embla__viewport" ref={emblaRef}>
+    <div
+      className="carousel embla"
+      style={{
+        width,
+        height,
+      }}
+    >
+      <header className="carousel-header">
+        {title && (
+          <div className="carousel-title">
+            {title}
+          </div>
+        )}
+        <div className="carousel-controls">
+          <div className="carousel-pagination">
+            {prev && (
+              <Button
+                className="embla__prev"
+                onClick={goToPrev}
+                icon="fas fa-angle-left"
+                circleIcon
+              />
+            )}
+            {next && (
+              <Button
+                className="embla__next"
+                onClick={goToNext}
+                icon="fas fa-angle-right"
+                circleIcon
+              />
+            )}
+          </div>
+        </div>
+      </header>
+      <div
+        ref={emblaRef}
+        className="embla__viewport"
+      >
         <div className="embla__container">
-          <div className="embla__slide">Slide 1</div>
-          <div className="embla__slide">Slide 2</div>
-          <div className="embla__slide">Slide 3</div>
+          {items.map((item) => {
+            return (
+              <div className="embla__slide">
+                {item}
+              </div>
+            )
+          })}
         </div>
       </div>
-
-      <button className="embla__prev" onClick={goToPrev}>Scroll to prev</button>
-      <button className="embla__next" onClick={goToNext}>Scroll to next</button>
     </div>
   )
 }
