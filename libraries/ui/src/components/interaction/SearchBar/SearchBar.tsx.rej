diff a/libraries/ui/src/components/interaction/SearchBar/SearchBar.tsx b/libraries/ui/src/components/interaction/SearchBar/SearchBar.tsx	(rejected hunks)
@@ -8,7 +8,6 @@ type SearchBarProps = {
   size?: 'l' | 'm',
   placeholder?: string,
   onChange?: (e, value) => void,
-  onSubmit?: (e) => void,
 }
 
 /**
@@ -18,7 +17,6 @@ const SearchBar = ({
   size = "m",
   placeholder = 'Search',
   onChange = () => {},
-  onSubmit,
 }: PropsWithChildren<SearchBarProps>) => {
   const [query, setQuery] = useState('')
 
@@ -29,18 +27,16 @@ const SearchBar = ({
 
   return (
     <div className={clsx('search-bar', `size-${size}`)}>
-      <form onSubmit={onSubmit}>
-        <label>
-          <i className="fas fa-search"></i>
-          <input
-            type="search"
-            name="query"
-            value={query}
-            placeholder={placeholder}
-            onChange={handleChange}
-          />
-        </label>
-      </form>
+      <label>
+        <i className="fas fa-search"></i>
+        <input
+          type="search"
+          name="query"
+          value={query}
+          placeholder={placeholder}
+          onChange={handleChange}
+        />
+      </label>
     </div>
   )
 }
