diff a/libraries/ui/src/components/features/AudioPlayer/AudioPlayer.tsx b/libraries/ui/src/components/features/AudioPlayer/AudioPlayer.tsx	(rejected hunks)
@@ -10,6 +10,7 @@ import Scrubber from '../../interaction/Scrubber'
 
 import { musicSelectors, musicActions } from '../../../store/slices/music'
 import { PLAYBACK_STATE } from '../../../store/slices/music/constants'
+import playMusic from '../../../store/slices/music/thunks/play'
 
 import { getHowl } from '../../../hooks/useHowler'
 import { useReleaseCover } from '../../../hooks/useReleaseCover'
@@ -20,6 +21,12 @@ import { secondsToMMSS } from '../../../lib/formatting/time'
 
 import './AudioPlayer.css'
 
+export const CACHED_SEEK_SESSION_STORAGE_KEY = '@cardinalapps/music_seek_cache'
+
+export type CachedSeekPositionType = {
+  [playerId: string]: number,
+}
+
 type AudioPlayerProps = {
   playerId: string,
   size: 'mini' | 'wide',
@@ -50,7 +57,7 @@ const AudioPlayer = ({
   const coverSrc = useReleaseCover(track?.release?.id)
 
   const handlePlayClick = (id) => {
-    dispatch(musicActions.play({ trackId: id }))
+    dispatch(playMusic({ trackId: id }))
   }
 
   const handlePauseClick = () => {
@@ -61,6 +68,22 @@ const AudioPlayer = ({
     dispatch(musicActions.stop(player.id))
   }
 
+  const getCachedSeekPositions = () => {
+    try {
+      return JSON.parse(sessionStorage.getItem(CACHED_SEEK_SESSION_STORAGE_KEY))
+    } catch (error) {
+      console.error(error)
+    }
+  }
+
+  const cacheSeekPosition = (playerId, seek) => {
+    const cached = getCachedSeekPositions()
+    sessionStorage.setItem(CACHED_SEEK_SESSION_STORAGE_KEY, JSON.stringify({
+      ...cached,
+      [playerId]: seek,
+    }))
+  }
+
   /**
    * Regularly update the current playback time.
    */
@@ -69,7 +92,7 @@ const AudioPlayer = ({
       playbackTimeInterval.current = setInterval(() => {
         const seek = howl.seek()
         setPlaybackSeconds(seek)
-        sessionStorage.setItem(`audio-player-${playerId}-seek`, seek)
+        cacheSeekPosition(playerId, seek)
       }, 250)
     }
 
@@ -119,7 +142,7 @@ const AudioPlayer = ({
             <Icon
               fa="fas fa-play"
               className="audio-player-playback-button play"
-              onClick={() => handlePlayClick(track?.id)}
+              onClick={() => handlePlayClick(track?.musicTrackId)}
             />
           }
           {!!isPlaying && !musicTrackLoading && !musicBlobLoading &&
