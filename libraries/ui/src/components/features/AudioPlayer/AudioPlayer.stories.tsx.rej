diff a/libraries/ui/src/components/features/AudioPlayer/AudioPlayer.stories.tsx b/libraries/ui/src/components/features/AudioPlayer/AudioPlayer.stories.tsx	(rejected hunks)
@@ -2,7 +2,8 @@ import type { Meta } from '@storybook/react'
 
 import Button from '../../interaction/Button'
 
-import { musicSelectors, musicActions } from '../../../store/slices/music'
+import { musicSelectors } from '../../../store/slices/music'
+import playMusic from '../../../store/slices/music/thunks/play'
 import { useAppDispatch } from '../../../hooks/useAppDispatch'
 import { useAppSelector } from '../../../hooks/useAppSelector'
 
@@ -10,6 +11,8 @@ import AudioPlayer from './AudioPlayer'
 
 import useHowler from '../../../hooks/useHowler'
 
+const samepleTrackId = '123'
+
 const meta = {
   title: 'Feature/AudioPlayer',
   component: AudioPlayer,
@@ -20,13 +23,13 @@ export const MiniPlayer = () => {
   useHowler()
   const dispatch = useAppDispatch()
   const players = useAppSelector(musicSelectors.players)
-  const player = Object.values(players).find((player) => player.trackId === 1)
+  const player = Object.values(players).find((player) => player.trackId === samepleTrackId)
 
   return (
     <>
       <div style={{ padding: '10px 0' }}>
         <p>Hardcoded to use track ID 1. Use the Music page in the sandbox app for more advanced testing.</p>
-        <p><Button onClick={() => dispatch(musicActions.play({ trackId: 1 }))}>Play track 1</Button></p>
+        <p><Button onClick={() => dispatch(playMusic({ trackId: samepleTrackId }))}>Play track 1</Button></p>
       </div>
       {!!player &&
         <AudioPlayer
