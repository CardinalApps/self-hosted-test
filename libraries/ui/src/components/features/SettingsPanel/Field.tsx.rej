diff a/libraries/ui/src/components/features/SettingsPanel/Field.tsx b/libraries/ui/src/components/features/SettingsPanel/Field.tsx	(rejected hunks)
@@ -21,6 +21,11 @@ function Field({
   field,
 }: PropsWithChildren<FieldProps>) {
   const { label, description, type, slug } = field
+
+  if (type === 'toggle') {
+    return children
+  }
+
   return (
     <div className={clsx('field', className, `type-${type}`, slug)} style={style}>
       <div className="field-meta">
