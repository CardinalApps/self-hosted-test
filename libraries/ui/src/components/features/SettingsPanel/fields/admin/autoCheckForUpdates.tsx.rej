diff a/libraries/ui/src/components/features/SettingsPanel/fields/admin/autoCheckForUpdates.tsx b/libraries/ui/src/components/features/SettingsPanel/fields/admin/autoCheckForUpdates.tsx	(rejected hunks)
@@ -2,8 +2,6 @@ import { getSetting } from '@cardinalapps/app-settings/src'
 
 import ToggleSwitch from '../../../../forms/ToggleSwitch'
 
-import Desc from '../../layout/Desc'
-
 const autoCheckForUpdates = (app, lang) => {
   const fieldFactory = getSetting('auto_check_for_updates')
   const field = fieldFactory(app, lang)
@@ -12,15 +10,12 @@ const autoCheckForUpdates = (app, lang) => {
     ...field,
     render: ({ value, onChange }) => {
       return (
-        <>
-          <ToggleSwitch
-            name={field.slug}
-            value={value}
-            label={field.label}
-            onChange={onChange}
-          />
-          {!!field?.description && <Desc type="toggle-switch">{field.description}</Desc>}
-        </>
+        <ToggleSwitch
+          value={value}
+          onChange={onChange}
+          showActiveLabel={true}
+          labelAlign='left'
+        />
       )
     },
   })
