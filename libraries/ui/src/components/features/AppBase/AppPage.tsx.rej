diff a/libraries/ui/src/components/features/AppBase/AppPage.tsx b/libraries/ui/src/components/features/AppBase/AppPage.tsx	(rejected hunks)
@@ -1,4 +1,4 @@
-import { useContext, useEffect } from 'react'
+import { useEffect } from 'react'
 import type { CSSProperties, PropsWithChildren, ReactNode } from 'react'
 import { useDispatch, useSelector } from 'react-redux'
 import clsx from 'clsx'
@@ -9,12 +9,11 @@ import {
   layoutSelectors,
   PAGE_LAYOUT,
   PAGE_BEHAVIORS,
-  SCROLL_RESTORATION_KEYS,
   SIDEBAR_MODE,
 } from '../../../store/slices/layout'
-import { RouterContext } from '../../../context/router'
 import HasCapabilities from '../../layout/HasCapabilities'
 import AccessError, { NetworkError } from '../../layout/AccessError/AccessError'
+import useScrollPointRestoration from '../../../hooks/useScrollPointRestoration'
 
 type AppPageProps = {
   layout?: string,
@@ -50,31 +49,12 @@ function AppPage({
   style,
   ...props
 }: PropsWithChildren<AppPageProps>) {
+  useScrollPointRestoration('.main-col', !restoreScrollPoint)
   const dispatch = useDispatch()
-  const { location } = useContext(RouterContext)
-  const scrollKey = `${SCROLL_RESTORATION_KEYS.page}:${location.pathname}`
-  const scrollPoints = useSelector(layoutSelectors.scrollPoints)
   const userSelectedSidebarMode = useSelector(layoutSelectors.userSelectedSidebarMode)
   const sidebarMode = useSelector(layoutSelectors.sidebarMode)
   const sidebarIsCollapsed = sidebarMode === SIDEBAR_MODE.collapsed
 
-  /**
-   * Restore page scroll.
-   */
-  useEffect(() => {
-    const el = document.querySelector('.main-col')
-
-    if (
-      el
-      && scrollPoints?.[scrollKey]
-      && restoreScrollPoint
-    ) {
-      setTimeout(() => {
-        el.scrollTop = scrollPoints[scrollKey] as number
-      }, 0)
-    }
-  }, [location.pathname])
-
   /**
    * App layout can change when the page changes.
    */
