diff a/libraries/ui/src/components/features/AppBase/AppPage.tsx b/libraries/ui/src/components/features/AppBase/AppPage.tsx	(rejected hunks)
@@ -18,6 +18,7 @@ type AppPageProps = {
   pageTitle?: string,
   pageDocLink?: string,
   className?: string | string[],
+  loading?: boolean,
   restoreScrollPoint?: boolean,
   toolbar?: ReactNode,
   virtualLayout?: ReactNode,
@@ -33,6 +34,7 @@ function AppPage({
   pageTitle,
   pageDocLink,
   className = [],
+  loading = false,
   children,
   restoreScrollPoint = true,
   toolbar,
@@ -84,7 +86,11 @@ function AppPage({
   }, [layout, pageTitle, pageDocLink])
 
   return (
-    <div {...props} style={style} className={clsx('app-page', className, sidebarIsCollapsed && 'sidebar-is-collapsed')}>
+    <div
+      {...props}
+      style={style}
+      className={clsx('app-page', className, sidebarIsCollapsed && 'sidebar-is-collapsed', loading && 'loading')}
+    >
       {!!toolbar && (
         <div className="page-toolbar">
           {toolbar}
