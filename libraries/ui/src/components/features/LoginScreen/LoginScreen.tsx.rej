diff a/libraries/ui/src/components/features/LoginScreen/LoginScreen.tsx b/libraries/ui/src/components/features/LoginScreen/LoginScreen.tsx	(rejected hunks)
@@ -19,7 +19,7 @@ import { homeServerUserSelectors } from '../../../store/slices/homeServerUser'
 import { settingsSelectors } from '../../../store/slices/settings'
 //import set from '../../../store/slices/settings/thunks/set'
 
-import { AppBasePaths } from '../../../lib/env/cardinal'
+import { AppBasePaths, CardinalApp } from '../../../lib/env/cardinal'
 
 import i18n from './i18n'
 
@@ -27,6 +27,7 @@ import './LoginScreen.css'
 import { BrandLogoIcon } from '../../layout/BrandLogo/BrandLogo'
 
 type LoginScreenProps = {
+  overrideApp?: CardinalApp,
   allowGuestAccount?: boolean,
   onLoginWithAnonymousAccountClick: () => void,
   cardinalSSOLoginButton: ReactNode,
@@ -39,13 +40,15 @@ type LoginScreenProps = {
  */
 const LoginScreen = ({
   //ownerAccount,
+  overrideApp,
   cardinalSSOLoginButton,
   allowGuestAccount = true,
   onLoginWithAnonymousAccountClick,
   onContinueAsCurrentUserClick,
 }: PropsWithChildren<LoginScreenProps>) => {
   //const dispatch = useDispatch()
-  const app = useSelector(appSelectors.app)
+  let app = useSelector(appSelectors.app)
+  if (overrideApp) app = overrideApp
   const kioskMode = useSelector(appSelectors.kioskMode)
   //const cloudUserLoggedIn = useSelector(cloudUserSelectors.loggedIn)
   const homeServerUserLoggedIn = useSelector(homeServerUserSelectors.loggedIn)
@@ -111,7 +114,7 @@ const LoginScreen = ({
         initial={{ opacity: 0 }}
         animate={{ opacity: 1, transition: { duration: 0.4 } }}
       >
-        <Card className="accounts" shadow={1}>
+        <Card className="accounts" shadow={2} border={3}>
           <header>
             <div className="logo">
               <BrandLogo
