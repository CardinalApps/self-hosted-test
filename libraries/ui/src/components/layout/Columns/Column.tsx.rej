diff a/libraries/ui/src/components/layout/Columns/Column.tsx b/libraries/ui/src/components/layout/Columns/Column.tsx	(rejected hunks)
@@ -12,6 +12,7 @@ type ColumnProps = {
   alignItems?: 'space-between' | 'space-around' | 'flex-start' | 'flex-end' | 'center',
   grow?: boolean,
   shrink?: boolean,
+  direction?: 'row' | 'column',
   className?: string,
   style?: CSSProperties,
   children?: ReactNode,
@@ -27,6 +28,7 @@ const Column = ({
   alignItems = 'center',
   grow = false,
   shrink = true,
+  direction = 'row',
   className = '',
   style,
   children,
@@ -43,6 +45,7 @@ const Column = ({
         ...(alignItems && { alignItems }),
         ...(minWidth && { minWidth }),
         ...(width && { width }),
+        ...(direction && { flexDirection: direction }),
         flexGrow: grow ? 1 : 0,
         flexShrink: shrink ? 1 : 0,
         ...style,
