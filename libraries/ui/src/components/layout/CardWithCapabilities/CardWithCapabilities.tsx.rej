diff a/libraries/ui/src/components/layout/CardWithCapabilities/CardWithCapabilities.tsx b/libraries/ui/src/components/layout/CardWithCapabilities/CardWithCapabilities.tsx	(rejected hunks)
@@ -1,6 +1,6 @@
 import { useMemo, type PropsWithChildren } from 'react'
 import clsx from 'clsx'
-import { getRole, hasCapabilities, NamedCapability } from '@cardinalapps/access-control/src'
+import { getMediaServerRole, hasCapabilities, MediaServerCapability } from '@cardinalapps/access-control/src'
 import AccessError from '../AccessError'
 
 import Card, { CardProps } from '../Card/Card'
@@ -10,7 +10,7 @@ import { homeServerUserSelectors } from '../../../store/slices/homeServerUser'
 import './CardWithCapabilities.css'
 
 type CardWithCapabilitiesProps = {
-  capabilities: NamedCapability[],
+  capabilities: MediaServerCapability[],
 } & CardProps
 
 /**
@@ -22,7 +22,7 @@ const CardWithCapabilities = (props: PropsWithChildren<CardWithCapabilitiesProps
   const currentUser = useAppSelector(homeServerUserSelectors.current)
 
   const hasAccess = useMemo(
-    () => hasCapabilities(props?.capabilities, currentUser?.roles?.flatMap((role) => getRole(role.role).capabilities)),
+    () => hasCapabilities(props?.capabilities, currentUser?.roles?.flatMap((role) => getMediaServerRole(role.role).capabilities)),
     [(props?.capabilities || []).length, currentUser.userId],
   )
 
