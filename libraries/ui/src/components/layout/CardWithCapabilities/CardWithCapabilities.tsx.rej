diff a/libraries/ui/src/components/layout/CardWithCapabilities/CardWithCapabilities.tsx b/libraries/ui/src/components/layout/CardWithCapabilities/CardWithCapabilities.tsx	(rejected hunks)
@@ -22,7 +22,7 @@ const CardWithCapabilities = (props: PropsWithChildren<CardWithCapabilitiesProps
   const currentUser = useAppSelector(homeServerUserSelectors.current)
 
   const hasAccess = useMemo(
-    () => hasCapabilities(props?.capabilities, currentUser?.roles?.flatMap((role) => getMediaServerRole(role.role).capabilities)),
+    () => hasCapabilities<MediaServerCapability>(props?.capabilities, currentUser?.roles?.flatMap((role) => getMediaServerRole(role.role).capabilities)),
     [(props?.capabilities || []).length, currentUser.userId],
   )
 
