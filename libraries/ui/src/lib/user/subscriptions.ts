/**
 * Keep the backend copy of this definition in sync. It is at:
 *
 * /server-auth/src/constants/subscriptions.js
 * 
 * @deprecated use @cardinalapps/products package instead
 */
const SUBSCRIPTIONS = {
  FREE: 'free',
  EARLY_ADOPTER: 'early_adopter',
}

export default Object.freeze(SUBSCRIPTIONS)
