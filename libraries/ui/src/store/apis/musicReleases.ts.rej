diff a/libraries/ui/src/store/apis/musicReleases.ts b/libraries/ui/src/store/apis/musicReleases.ts	(rejected hunks)
@@ -90,9 +90,10 @@ export const musicReleasesApi = baseHomeServerApi
           thumbnails?: boolean,
           order?: CommonOrderParams,
           orderBy?: 'name' | 'createdAt',
+          libraries?: string[],
         }
       >({
-        query: ({ take, skip, order, orderBy, tracks, genres, artists, thumbnails }) => {
+        query: ({ take, skip, order, orderBy, tracks, genres, artists, thumbnails, libraries }) => {
           return queryParams('/music/releases', {
             ...(take && { take }),
             ...(skip && { skip }),
@@ -102,6 +103,7 @@ export const musicReleasesApi = baseHomeServerApi
             ...(genres && { genres }),
             ...(artists && { artists }),
             ...(thumbnails && { thumbnails }),
+            ...(libraries && { libraries }),
           })
         },
       }),
