diff a/libraries/ui/src/store/apis/musicTracks.ts b/libraries/ui/src/store/apis/musicTracks.ts	(rejected hunks)
@@ -6,6 +6,7 @@ import { baseApi } from './base'
 export type TracksSortParams = CommonSortParams | 'sortTitle' | 'trackNumber'
 export type MusicTrackType = {
   id: number,
+  musicTrackId: string,
   title: string,
   trackNumber: number,
   discNumber: number,
@@ -93,7 +94,7 @@ export const musicTracksApi = baseApi
        */
       getMusicTrack: builder.query<
         MusicTrackType,
-        { id: number }
+        { id: string }
       >({
         query: ({ id }) => {
           return queryParams(`/music/track/${id}`)
