diff a/libraries/ui/src/store/apis/playbackQueues.ts b/libraries/ui/src/store/apis/playbackQueues.ts	(rejected hunks)
@@ -5,7 +5,11 @@ import { UserType } from '../../types/user'
 
 import { CommonOrderParams, PaginationParams } from '../types/api'
 import { baseHomeServerApi } from './baseHomeServerApi'
+import { QueueItem, ServerQueue } from '../slices/music'
 
+/**
+ * @deprecated
+ */
 export type QueueT = {
   createdAt?: string,
   user: UserType,
@@ -19,6 +23,18 @@ export const playbackQueueApi = baseHomeServerApi
   })
   .injectEndpoints({
     endpoints: (builder) => ({
+      getQueue: builder.query<
+        ServerQueue,
+        {
+          queueId: string,
+        }
+      >({
+        query: ({
+          queueId,
+        }) => queryParams(`/playback-queues/${queueId}`, {
+        }),
+      }),
+
       getQueues: builder.query<
         [QueueT[], number],
         PaginationParams & {
@@ -66,6 +99,7 @@ export const playbackQueueApi = baseHomeServerApi
 
 export const {
   useGetQueuesQuery,
+  useGetQueueItemsQuery,
   useCreateQueueMutation,
   useDeleteQueueMutation,
 } = playbackQueueApi
