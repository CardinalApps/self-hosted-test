diff a/libraries/ui/src/store/apis/musicHistory.ts b/libraries/ui/src/store/apis/musicHistory.ts	(rejected hunks)
@@ -8,6 +8,7 @@ import { MusicTrackType } from './musicTracks'
 export type MusicHistorySortParams = CommonSortParams | 'sortTitle' | 'trackNumber'
 export type MusicHistoryEntryType = {
   createdAt: string,
+  updatedAt: string,
   id: number,
   progress: number,
   playbackEntryId: string,
@@ -15,6 +16,10 @@ export type MusicHistoryEntryType = {
   user: UserType,
   [key: string]: unknown,
 }
+export type UpsertMusicHistoryEntryType = {
+  queueEntryId: string,
+  seconds: number,
+}
 
 export const musicHistoryApi = baseHomeServerApi
   .enhanceEndpoints({
@@ -56,10 +61,27 @@ export const musicHistoryApi = baseHomeServerApi
             ...(metadata && { metadata }),
           })
         },
+        providesTags: ['MusicHistoryList'],
+      }),
+
+      /**
+       * Mutations.
+       */
+      upsertHistoryEntry: builder.mutation<
+        UpsertMusicHistoryEntryType,
+        Partial<MusicHistoryEntryType>
+      >({
+        query: (body) => ({
+          url: '/music/history',
+          method: 'PATCH',
+          body,
+        }),
+        invalidatesTags: ['MusicHistoryList'],
       }),
     }),
   })
 
 export const {
+  useUpsertHistoryEntryMutation,
   useGetInfiniteMusicHistoryInfiniteQuery,
 } = musicHistoryApi
