diff a/libraries/ui/src/store/apis/musicArtists.ts b/libraries/ui/src/store/apis/musicArtists.ts	(rejected hunks)
@@ -45,7 +45,7 @@ export const musicArtistsApi = baseApi
           return queryParams('/music/artists', {
             ...(typeof skip !== 'undefined' && { skip }),
             ...(take && { take }),
-            ...(sort && { order }),
+            ...(sort && { sort }),
             ...(order && { order }),
             releases: true,
             tracks: true,
