diff a/libraries/ui/src/store/apis/homeServerUsers.ts b/libraries/ui/src/store/apis/homeServerUsers.ts	(rejected hunks)
@@ -27,12 +27,12 @@ export const usersApi = baseHomeServerApi
       getUsers: builder.query<
         [UserType[], number],
         PaginationParams & {
-          roles: boolean,
+          roles?: boolean,
         }
       >({
         query: ({ roles }) => {
           return queryParams('/users', {
-            roles,
+            ...(roles ? { roles } : {}),
           })
         },
         providesTags: ['Users.List'],
