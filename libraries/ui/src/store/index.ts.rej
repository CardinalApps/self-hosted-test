diff a/libraries/ui/src/store/index.ts b/libraries/ui/src/store/index.ts	(rejected hunks)
@@ -31,6 +31,7 @@ import { musicTracksApi } from './apis/musicTracks'
 import { musicReleasesApi } from './apis/musicReleases'
 import { musicArtistsApi } from './apis/musicArtists'
 import { runsApi } from './apis/runs'
+import { versionsApi } from './apis/versions'
 
 /**
  * Middleware
@@ -79,6 +80,7 @@ const slices = {
   [musicTracksApi.reducerPath]: musicTracksApi,
   [musicReleasesApi.reducerPath]: musicReleasesApi,
   [musicArtistsApi.reducerPath]: musicArtistsApi,
+  [versionsApi.reducerPath]: versionsApi,
 }
 
 const rootReducer = combineSlices(...Object.values(slices))
@@ -114,6 +116,7 @@ const createStore = () => {
         musicTracksApi.middleware,
         musicReleasesApi.middleware,
         musicArtistsApi.middleware,
+        versionsApi.middleware,
         resetMiddleware.middleware,
         sseFactoryResetMiddleware.middleware,
         sseLatestEventMiddleware.middleware,
