diff a/libraries/ui/src/store/slices/homeServer/index.ts b/libraries/ui/src/store/slices/homeServer/index.ts	(rejected hunks)
@@ -17,6 +17,7 @@ type HomeServerSliceState = {
   latestEvent: Record<string, unknown> | null,
   firstTimeSetupComplete: boolean,
   instanceId: string | null,
+  serverName: string | null,
 }
 
 const initialState: HomeServerSliceState = {
@@ -28,6 +29,7 @@ const initialState: HomeServerSliceState = {
   latestEvent: null,
   firstTimeSetupComplete: null,
   instanceId: null,
+  serverName: null,
 }
 
 const homeServerSlice = createSlice({
@@ -57,7 +59,7 @@ const homeServerSlice = createSlice({
       })
       .addCase(healthCheck.fulfilled, (state, action: PayloadAction<{ state: string }>) => {
         // const resObj = {
-        //   response: payload,
+        //   response: payload
         //   timestamp: Date.now(),
         // }
         state.health.loading = false
