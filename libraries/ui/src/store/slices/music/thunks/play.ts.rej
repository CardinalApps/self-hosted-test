diff a/libraries/ui/src/store/slices/music/thunks/play.ts b/libraries/ui/src/store/slices/music/thunks/play.ts	(rejected hunks)
@@ -119,6 +119,12 @@ const playMusic = createAsyncThunk<
           console.error('Could not start playback because the first item in the queue is either missing or not a music track.', firstItem)
         }
       }
+
+      // Don't save the queue items so that they cannot be used accidentially;
+      // always use the server state instead
+      if ('items' in queue) {
+        delete queue.items
+      }
     }
 
     workToDo.create?.push({
