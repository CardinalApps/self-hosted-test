diff a/libraries/ui/src/store/slices/cloudUser/thunks/fetch.ts b/libraries/ui/src/store/slices/cloudUser/thunks/fetch.ts	(rejected hunks)
@@ -1,11 +1,11 @@
 import { createAsyncThunk } from '@reduxjs/toolkit'
 
-import { getJWT, deleteJwt, JWT_TYPE, authorizedFetchHeaders } from '../../../../lib/auth/jwt'
+import { deleteJwt } from '../../../../lib/auth/jwt'
 
-import { STORE_KEY, USER_DATA_API_URL, USER_DATA_PII_API_URL } from '../constants'
+import { STORE_KEY } from '../constants'
 import { AppDispatch, RootState } from '../../..'
 import { CloudUser } from '..'
-//import authAPI from '../../../../lib/auth/authAPI'
+import authAPI from '../../../../lib/auth/authAPI'
 
 export type FetchCloudUserSuccess = {
   status: string | number | boolean,
@@ -41,16 +41,6 @@ const fetchCloudUser = createAsyncThunk<
     state: RootState
   }
 >(`${STORE_KEY}/fetchUser`, async (options?) => {
-
-  // const test = await authAPI('/user?meeee')
-
-  // console.log('TEST', test)
-
-  if (!getJWT(JWT_TYPE.CLOUD_USER)) {
-    console.error('fetchUser requires that the app has a JWT.')
-    throw new Error()
-  }
-
   if (!options) {
      options = { url: null, piiUrl: null }
   }
@@ -60,12 +50,8 @@ const fetchCloudUser = createAsyncThunk<
   // Try to get the user's public and private data
   try {
     responses = await Promise.all([
-      fetch(options?.url ? options.url : USER_DATA_API_URL, {
-        headers: { ...authorizedFetchHeaders(JWT_TYPE.CLOUD_USER) },
-      }),
-      fetch(options?.piiUrl ? options.piiUrl : USER_DATA_PII_API_URL, {
-        headers: { ...authorizedFetchHeaders(JWT_TYPE.CLOUD_USER) },
-      }),
+      authAPI('/user', 'GET', { returnRawResponse: true }),
+      authAPI('/user/pii', 'GET', { returnRawResponse: true }),
     ])
   } catch (error) {
     console.warn('No response from backend.')
