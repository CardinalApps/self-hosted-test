diff a/libraries/ui/src/store/slices/homeServerUser/thunks/login.ts b/libraries/ui/src/store/slices/homeServerUser/thunks/login.ts	(rejected hunks)
@@ -12,6 +12,8 @@ import { AppDispatch, RootState } from '../../../'
 
 export type HomeServerLoginArgs = {
   userId?: string,
+  username?: string,
+  password?: string,
   cardinalSSOToken?: string,
   redirectOutOfNextLoginPageVisit?: boolean
 }
@@ -42,7 +44,15 @@ const homeServerLogin = createAsyncThunk<
     state: RootState
     rejectValue: { error: string }
   }
->(`${STORE_KEY}/login`, async ({ userId, cardinalSSOToken, redirectOutOfNextLoginPageVisit = true }, thunkAPI): Promise<HomeServerLoginResponse> => {
+>(`${STORE_KEY}/login`, async (args, thunkAPI): Promise<HomeServerLoginResponse> => {
+  const {
+    userId,
+    username,
+    password,
+    cardinalSSOToken,
+    redirectOutOfNextLoginPageVisit = true,
+  } = args
+
   let user
   let homeServerJWT
   let cloudUser
@@ -56,6 +66,8 @@ const homeServerLogin = createAsyncThunk<
       },
       body: {
         userId,
+        username,
+        password,
         cardinalJWT: cardinalSSOToken,
       },
     }) as { user, JWT, cloudUser, cloudJWT }
