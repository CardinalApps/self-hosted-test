diff a/apps/music-web/src/pages/Release/Release.tsx b/apps/music-web/src/pages/Release/Release.tsx	(rejected hunks)
@@ -24,8 +24,6 @@ function ReleasePage() {
     // isSuccess,
   } = useGetMusicReleaseQuery({ id: releaseId })
 
-  console.log(data)
-
   const artists = data?.artists || []
   //const genres = data?.genres || []
 
@@ -59,9 +57,9 @@ function ReleasePage() {
       <Columns grow={true} gap="var(--gutter)" alignItems='flex-start'>
         <Column minWidth={340}>
           <MusicRelease
+            hasArtwork={!!data?.thumbnails?.length}
             releaseId={releaseId}
-            tracks={data?.tracks || []}
-            playButtonSize="m"
+            tracks={data?.tracks as MusicTrackType[] || []}
             coverSize={{
               width: 340,
               height: 340,
@@ -79,6 +77,7 @@ function ReleasePage() {
                   releaseId={musicTrack?.release?.id}
                   trackNumber={musicTrack.trackNumber}
                   artistName={artists?.[0]?.name as string}
+                  hasArtwork={!!musicTrack.thumbnails?.length}
                 />
               )
             })
