diff a/apps/music-web/src/pages/Release/Release.tsx b/apps/music-web/src/pages/Release/Release.tsx	(rejected hunks)
@@ -9,6 +9,7 @@ import { PAGE_LAYOUT } from '@cardinalapps/ui/src/store/slices/layout/constants'
 import { useGetMusicReleaseQuery } from '@cardinalapps/ui/src/store/apis/musicReleases'
 import { MusicTrackType } from '@cardinalapps/ui/src/store/apis/musicTracks'
 import H1 from '@cardinalapps/ui/src/components/typography/H1'
+import ErrorPage, { ErrorObject } from '@cardinalapps/ui/src/components/layout/ErrorPage/ErrorPage'
 
 import ReleaseArtists from './ReleaseArtists'
 import ReleaseMeta from './ReleaseMeta'
@@ -24,8 +25,8 @@ function ReleasePage() {
   const releaseId = params?.id as string
   const {
     data,
-    // isLoading,
-    // isSuccess,
+    isLoading,
+    error,
   } = useGetMusicReleaseQuery({ id: releaseId })
 
   const artists = data?.artists || []
@@ -51,11 +52,16 @@ function ReleasePage() {
     return groupedByDisc.map((disc) => disc.sort((a: MusicTrackType, b: MusicTrackType) => a.trackNumber - b.trackNumber))
   }, [data?.tracks])
 
+  if (error) {
+    return <ErrorPage error={error as ErrorObject} />
+  }
+
   return (
     <AppPage
       className="music-release-page"
       layout={PAGE_LAYOUT.standard}
       pageTitle={i18n['music-release.title']['en']}
+      loading={isLoading}
     >
       <H1 className="release-name">{data?.title}</H1>
       <div className="release-page-cols">
