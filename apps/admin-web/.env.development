# Enables the `window.__testDispatch` Redux seam in `main.tsx` so the e2e suite
# can replay `sse/*` events without standing up a real EventSource. Vite's
# dead-code elimination strips the seam from production builds because this
# variable is only loaded in dev mode.
VITE_E2E=true
