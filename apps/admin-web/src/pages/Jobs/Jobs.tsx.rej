diff a/apps/admin-web/src/pages/Jobs/Jobs.tsx b/apps/admin-web/src/pages/Jobs/Jobs.tsx	(rejected hunks)
@@ -1,23 +1,17 @@
-import { useState, useEffect } from 'react'
-import { useSelector, useDispatch } from 'react-redux'
+import { useState } from 'react'
+import { useSelector } from 'react-redux'
 import { motion, AnimatePresence, Variants } from 'framer-motion'
 import clsx from 'clsx'
 
 import AppPage from '@cardinalapps/ui/src/components/features/AppBase/AppPage'
-import H1 from '@cardinalapps/ui/src/components/typography/H1'
-import H3 from '@cardinalapps/ui/src/components/typography/H3'
-import Card from '@cardinalapps/ui/src/components/layout/Card'
-import Loading from '@cardinalapps/ui/src/components/layout/Loading'
 
 import { settingsSelectors } from '@cardinalapps/ui/src/store/slices/settings'
-import { toastActions } from '@cardinalapps/ui/src/store/slices/toast'
-import { homeServerSelectors } from '@cardinalapps/ui/src/store/slices/homeServer'
 
 import StartJob from './components/StartJob'
-import ActiveJob from './components/ActiveJob'
-import JobReport from './components/JobReport'
-
-import { jobActions } from '@cardinalapps/ui/src/store/slices/jobs'
+import CardGrid from '@cardinalapps/ui/src/components/layout/CardGrid'
+import { PAGE_LAYOUT } from '@cardinalapps/ui/src/store/slices/layout'
+import Active from './cards/Active'
+import History from './cards/History'
 
 import { useGetJobTypesQuery, useGetJobsQuery } from '@cardinalapps/ui/src/store/apis/jobs'
 
@@ -31,30 +25,16 @@ export type JobStatus = 'in_queue' | 'preparing' | 'running' | 'paused' | 'cance
 // }
 
 function Jobs() {
-  const dispatch = useDispatch()
   const { lang } = useSelector(settingsSelectors.current)
   const [jobToStart, setJobToStart] = useState()
-  const latestEvent = useSelector(homeServerSelectors.latestEvent)
 
   const {
     data: jobTypes,
   } = useGetJobTypesQuery()
 
   const {
-    data: activeJobsResponse,
-    isLoading: activeJobsLoading,
-    isError: activeJobsError,
     refetch: refetchActiveJobs,
   } = useGetJobsQuery({ take: 9999, skip: 0, status: ['preparing', 'paused', 'running', 'in_queue'], order: 'asc' })
-  const [activeJobs] = activeJobsResponse || []
-
-  const {
-    data: jobReportsResponse,
-    isLoading: jobReportsLoading,
-    isError: jobReportsError,
-    refetch: refetchJobReports,
-  } = useGetJobsQuery({ take: 9999, skip: 0, status: ['canceled', 'completed', 'errored'], order: 'desc' })
-  const [jobReports] = jobReportsResponse || []
 
   const handleJobTypeClick = (job) => {
     if (jobToStart === job) {
@@ -90,147 +70,48 @@ function Jobs() {
     },
   }
 
-  /**
-   * Clear stale cached job progress when we know of new active jobs.
-   */
-  useEffect(() => {
-    if (activeJobs?.length) {
-      dispatch(jobActions.clearActiveJobProgress({
-        except: activeJobs,
-      }))
-    }
-  }, [activeJobs])
-
-  /**
-   * Refetch data when jobs trigger events on the server.
-   */
-  useEffect(() => {
-    switch (latestEvent?.type) {
-      case 'sse/job.preparing':
-      case 'sse/job.prepared':
-      case 'sse/job.started':
-        refetchActiveJobs()
-        break
-
-      case 'sse/job.completed':
-        refetchActiveJobs()
-        refetchJobReports()
-        break
-    }
-  }, [latestEvent])
-
-  useEffect(() => {
-    if (activeJobsError) {
-      dispatch(toastActions.addToQueue({
-        type: 'danger',
-        ttl: 8000,
-        title: i18n['jobs.error.could-not-fetch-active-jobs'][lang],
-        showClose: true,
-      }))
-    }
-    if (jobReportsError) {
-      dispatch(toastActions.addToQueue({
-        type: 'danger',
-        ttl: 8000,
-        title: i18n['jobs.error.could-not-fetch-job-history'][lang],
-        showClose: true,
-      }))
-    }
-  }, [activeJobsError, jobReportsError])
-
   return (
-    <AppPage>
-      <div className={'jobs'}>
-        <div className={'wrapper'}>
-          <H1>{i18n['title'][lang]}</H1>
-
-          <div className={'jobTags'}>
-            {!!jobTypes?.length && jobTypes.map((job) => {
-              const configuring = jobToStart === job
-              return (
-                <button
-                  key={job}
-                  className={clsx('jobTag', configuring && 'configuring')}
-                  onClick={() => handleJobTypeClick(job)}
-                >
-                  {configuring
-                    ? <i className="fas fa-minus-circle" />
-                    : <i className="fas fa-plus-circle" />
-                  }
-                  <span>{i18n[`job.${job}.name`]?.['en'] || job}</span>
-                </button>
-              )
-            })}
-          </div>
-
-          <AnimatePresence>
-            {!!jobToStart &&
-              <motion.div
-                initial="initial"
-                animate="open"
-                exit="exit"
-                variants={newJobVariants as Variants}
-              >
-                <StartJob
-                  jobType={jobToStart}
-                  refetchActiveJobs={refetchActiveJobs}
-                  close={() => setJobToStart(undefined)}
-                />
-              </motion.div>
-            }
-          </AnimatePresence>
-
-          <header className={'activeJobsHeader'}>
-            <H3 className={'title'}>{i18n['active-jobs.title'][lang]}</H3>
-            {/* <i className="far fa-question-circle" title={i18n['active-jobs.help-tooltip'][lang]} /> */}
-          </header>
-          <Card
-            bg={1}
-            border={1}
-            padding={'none'}
-            className={'jobQueue'}
-          >
-            {!!activeJobsLoading && <div className={'loading'}><Loading /></div>}
-            {!activeJobsLoading && !!activeJobs?.length
-              ?
-              activeJobs.map((job) => {
-                return (
-                  <ActiveJob
-                    key={job.jobId}
-                    job={job}
-                  />
-                )
-              })
-              : <div className={'noActiveJobs'} dangerouslySetInnerHTML={{ __html: i18n['active-jobs.no-active-jobs'][lang] }} />
-            }
-          </Card>
-
-          <header className={'jobHistoryHeader'}>
-            <H3 className={'title'}>{i18n['job-reports.title'][lang]}</H3>
-            {/* <i className="far fa-question-circle" title={i18n['active-jobs.help-tooltip'][lang]} /> */}
-          </header>
-          <Card
-            bg={1}
-            border={1}
-            padding={'none'}
-            className={'jobReports'}
-          >
-            {!!jobReportsLoading && <div className={'loading'}><Loading /></div>}
-            {!jobReportsLoading && !!jobReports?.length
-              ?
-              jobReports.map((job) => {
-                return (
-                  <JobReport
-                    key={job.jobId}
-                    job={job}
-                  />
-                )
-              })
-              : <div className={'noActiveJobs'} dangerouslySetInnerHTML={{ __html: i18n['job-reports.no-active-jobs'][lang] }} />
-            }
-          </Card>
-        </div>
+    <AppPage layout={PAGE_LAYOUT.standard} pageTitle={i18n['title'][lang]}>
+      <div className={'jobTags'}>
+        {!!jobTypes?.length && jobTypes.map((job) => {
+          const configuring = jobToStart === job
+          return (
+            <button
+              key={job}
+              className={clsx('jobTag', configuring && 'configuring')}
+              onClick={() => handleJobTypeClick(job)}
+            >
+              {configuring
+                ? <i className="fas fa-minus-circle" />
+                : <i className="fas fa-plus-circle" />
+              }
+              <span>{i18n[`job.${job}.name`]?.['en'] || job}</span>
+            </button>
+          )
+        })}
       </div>
+
+      <AnimatePresence>
+        {!!jobToStart &&
+          <motion.div
+            initial="initial"
+            animate="open"
+            exit="exit"
+            variants={newJobVariants as Variants}
+          >
+            <StartJob
+              jobType={jobToStart}
+              refetchActiveJobs={refetchActiveJobs}
+              close={() => setJobToStart(undefined)}
+            />
+          </motion.div>
+        }
+      </AnimatePresence>
+
+      <CardGrid rowHeight='l'>
+        <Active />
+        <History />
+      </CardGrid>
     </AppPage>
   )
 }
