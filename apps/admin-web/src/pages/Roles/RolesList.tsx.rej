diff a/apps/admin-web/src/pages/Roles/RolesList.tsx b/apps/admin-web/src/pages/Roles/RolesList.tsx	(rejected hunks)
@@ -46,14 +46,14 @@ function RolesList() {
   /**
    * Return all users with the given role.
    */
-  const usersWithRole = (role) => roleAssignments
+  const usersWithRole = (role: string) => roleAssignments
     .filter((assignment: RoleAssignmentEntity) => assignment.role === role)
-    .map((assignment) => users.find((user) => user?.userId === assignment.user?.userId))
+    .map((assignment) => assignment.user)
 
   /**
    * Return all users that don't have the given role.
    */
-  const usersWithoutRole = (role) => users.filter((user: UserType) => {
+  const usersWithoutRole = (role: string) => users.filter((user: UserType) => {
     const userRoleAssignments = roleAssignments.filter((assignment) => assignment.user?.userId === user?.userId)
 
     // Never show the owner in the available users
@@ -158,7 +157,7 @@ function RolesList() {
               name: user?.designation === 'guest_account'
                 ? i18n['roles.drawer.user-list.guest'][lang]
                 :  user?.cachedCloudUser?.publicName || 'Cardinal User',
-              value: user.userId,
+              value: user?.userId,
               canDelete: user?.designation !== 'guest_account',
               avatar: user?.designation === 'guest_account'
                 ? { type: 'guest' }
@@ -173,7 +172,7 @@ function RolesList() {
               name: user?.designation === 'guest_account'
                 ? i18n['roles.drawer.user-list.guest'][lang]
                 :  user?.cachedCloudUser?.publicName || 'Cardinal User',
-              value: user.userId,
+              value: user?.userId,
               avatar: user?.designation === 'guest_account'
               ? { type: 'guest' }
               : user?.cachedCloudUser?.avatar
@@ -183,7 +182,7 @@ function RolesList() {
         }
       />
     )
-  }, [drawerRoleSlug, roleAssignments.length])
+  }, [drawerRoleSlug, roleAssignments.length, users.length])
 
   return (
     <CardGrid.Card
