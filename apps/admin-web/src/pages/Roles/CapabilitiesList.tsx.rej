diff a/apps/admin-web/src/pages/Roles/CapabilitiesList.tsx b/apps/admin-web/src/pages/Roles/CapabilitiesList.tsx	(rejected hunks)
@@ -4,8 +4,8 @@ import {
   Capabilities,
   Roles,
   hasCapability,
-  Components,
-  ComponentNames,
+  Aspects,
+  AspectNames,
   RoleNames,
 } from '@cardinalapps/access-control/src'
 
@@ -24,16 +24,16 @@ import { settingsSelectors } from '@cardinalapps/ui/src/store/slices/settings'
 import i18n from './i18n.json'
 
 type CapabilitiesListProps = {
-  globallySelectedComponent: ComponentNames,
+  globallySelectedAspect: AspectNames,
   globallySelectedRole: RoleNames,
-  setGloballySelectedComponent: (ComponentNames) => void
+  setGloballySelectedAspect: (AspectNames) => void
   setGloballySelectedRole: (RoleNames) => void
 }
 
 function CapabilitiesList({
-  globallySelectedComponent,
+  globallySelectedAspect: globallySelectedAspect,
   globallySelectedRole,
-  setGloballySelectedComponent,
+  setGloballySelectedAspect,
   setGloballySelectedRole,
 }: CapabilitiesListProps) {
   const { lang } = useAppSelector(settingsSelectors.current)
@@ -46,12 +46,12 @@ function CapabilitiesList({
       i18n['caps.plural'][lang],
     )
 
-    if (!globallySelectedComponent && !globallySelectedRole) {
+    if (!globallySelectedAspect && !globallySelectedRole) {
       return i18n['caps-list.total'][lang]
         .replace('{total}', Capabilities.length)
     }
 
-    if (!globallySelectedComponent && globallySelectedRole) {
+    if (!globallySelectedAspect && globallySelectedRole) {
       return i18n['caps-list.only-role'][lang]
         .replace('{role}', ACi18n[`role.${globallySelectedRole}.name`]?.[lang])
         .replace('{num}', filteredItems.length)
@@ -59,18 +59,18 @@ function CapabilitiesList({
         .replace('{capabilities}', pluralizedCapability)
     }
 
-    if (globallySelectedComponent && !globallySelectedRole) {
-      return i18n['caps-list.only-component'][lang]
-        .replace('{component}', globallySelectedComponent)
+    if (globallySelectedAspect && !globallySelectedRole) {
+      return i18n['caps-list.only-aspect'][lang]
+        .replace('{aspect}', globallySelectedAspect)
         .replace('{num}', filteredItems.length)
         .replace('{total}', Capabilities.length)
         .replace('{capabilities}', pluralizedCapability)
     }
 
-    if (globallySelectedComponent && globallySelectedRole) {
-      return i18n['caps-list.component-and-role'][lang]
+    if (globallySelectedAspect && globallySelectedRole) {
+      return i18n['caps-list.aspect-and-role'][lang]
         .replace('{role}', ACi18n[`role.${globallySelectedRole}.name`]?.[lang])
-        .replace('{component}', globallySelectedComponent)
+        .replace('{aspect}', globallySelectedAspect)
         .replace('{num}', filteredItems.length)
         .replace('{total}', Capabilities.length)
         .replace('{capabilities}', pluralizedCapability)
@@ -87,8 +87,8 @@ function CapabilitiesList({
         }
       })
       .filter((cap) => {
-        if (globallySelectedComponent) {
-          return !!cap.includes(globallySelectedComponent)
+        if (globallySelectedAspect) {
+          return !!cap.includes(globallySelectedAspect)
         } else {
           return true
         }
@@ -102,7 +102,7 @@ function CapabilitiesList({
       })
 
     setFilteredItems(filtered)
-  }, [globallySelectedComponent, globallySelectedRole])
+  }, [globallySelectedAspect, globallySelectedRole])
 
   return (
     <CardGrid.Card
@@ -129,19 +129,19 @@ function CapabilitiesList({
               onChange={(selected) => setGloballySelectedRole(selected)}
             />
             <Select
-              name="component"
+              name="aspect"
               size='s'
               layout="underline"
               multi={false}
-              selectPlaceholder={i18n['caps-list.component-filter'][lang]}
-              value={globallySelectedComponent}
-              options={Components.map((component) => {
+              selectPlaceholder={i18n['caps-list.aspect-filter'][lang]}
+              value={globallySelectedAspect}
+              options={Aspects.map((aspect) => {
                 return {
-                  label: component,
-                  value: component,
+                  label: aspect,
+                  value: aspect,
                 }
               })}
-              onChange={(selected) => setGloballySelectedComponent(selected)}
+              onChange={(selected) => setGloballySelectedAspect(selected)}
             />
           </>
         </Form>
