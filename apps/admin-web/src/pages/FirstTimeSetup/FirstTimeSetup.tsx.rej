diff a/apps/admin-web/src/pages/FirstTimeSetup/FirstTimeSetup.tsx b/apps/admin-web/src/pages/FirstTimeSetup/FirstTimeSetup.tsx	(rejected hunks)
@@ -111,7 +111,7 @@ function FirstTimeSetup() {
    * Exit the First Time Setup once the setup is complete.
    */
   useEffect(() => {
-    if (homeServerUserLoggedIn) {
+    if (homeServerUserLoggedIn && health !== 'not_setup') {
       dispatch(homeServerActions.setFirstTimeSetupComplete(true))
       navigate(routes.LOGIN)
     }
