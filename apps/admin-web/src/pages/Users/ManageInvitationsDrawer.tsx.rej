diff a/apps/admin-web/src/pages/Users/ManageInvitationsDrawer.tsx b/apps/admin-web/src/pages/Users/ManageInvitationsDrawer.tsx	(rejected hunks)
@@ -8,6 +8,9 @@ import SearchBar from '@cardinalapps/ui/src/components/interaction/SearchBar'
 import Confirm from '@cardinalapps/ui/src/components/interaction/Confirm'
 import Button from '@cardinalapps/ui/src/components/interaction/Button'
 import Textarea from '@cardinalapps/ui/src/components/forms/Textarea'
+import DatePicker from '@cardinalapps/ui/src/components/forms/DatePicker'
+import Form from '@cardinalapps/ui/src/components/forms/Form'
+import FormField from '@cardinalapps/ui/src/components/forms/FormField'
 
 import { settingsSelectors } from '@cardinalapps/ui/src/store/slices/settings'
 //import { formatDate, formatTimeAgo } from '@cardinalapps/ui/src/lib/formatting/time'
@@ -38,22 +41,20 @@ function ManageInvitationsDrawer({ onClose }: ManageInvitationsDrawerProps) {
     setUserToInvite(val)
   }
 
-  console.log(createInvitationResult)
-
-  const handleCreateInvitiationLink = () => {
-    setGeneratedLink('https://account.cardinalapps.io/join/jfi9nd3f')
+  const handleCreateInvitiationLink = (e, values) => {
+    createInvitation({
+      type: 'link',
+      expiresAt: new Date(values?.expiration),
+    })
   }
 
   useEffect(() => {
     if (createInvitationResult.isSuccess) {
-      setGeneratedLink(createInvitationResult.data?.link)
+      //setGeneratedLink(createInvitationResult.data?.link)
+      setGeneratedLink('https://account.cardinalapps.io/join/f8s2easm')
     }
-    createInvitation({})
   }, [createInvitationResult.isSuccess])
 
-  console.log(userToInvite)
-  //console.log(res)
-
   return (
     <>
       <Drawer
@@ -83,14 +84,30 @@ function ManageInvitationsDrawer({ onClose }: ManageInvitationsDrawerProps) {
 
           <Drawer.Tab index={1}>
             <Drawer.Section className="create-invitiation-link">
-              <p className="">{i18n['users.invite.link.create.notice'][lang]}</p>
-              <Button
-                className="create-invitation-link"
-                type="submit"
-                onClick={() => handleCreateInvitiationLink()}
+              <p>{i18n['users.invite.link.create.notice'][lang]}</p>
+              <Form
+                onSubmit={(e, values) => handleCreateInvitiationLink(e, values)}
               >
-                {i18n['users.invite.link.generate'][lang]}
-              </Button>
+                <FormField label={i18n['users.invite.link.create.expiration'][lang]} labelFor="invitation-link-expiration">
+                  {/* TODO add time picker */}
+                  <DatePicker
+                    id="invitation-link-expiration"
+                    name="expiration"
+                    min={new Date().toString()}
+                    required={true}
+                  />
+                </FormField>
+                <Button
+                  className="create-invitation-link"
+                  type="submit"
+                  animation={createInvitationResult.originalArgs?.type === 'link' && createInvitationResult.isLoading
+                    ? 'loading'
+                    : null
+                  }
+                >
+                  {i18n['users.invite.link.generate'][lang]}
+                </Button>
+              </Form>
               {!!generatedLink && (
                 <Textarea
                   className="generated-invitation-link"
@@ -105,12 +122,14 @@ function ManageInvitationsDrawer({ onClose }: ManageInvitationsDrawerProps) {
             {!data?.length && <p className="no-invitations">{i18n['users.invite-links.empty'][lang]}</p>}
             {!!data?.length && (
               <List
-                items={[
-                  {
-                    name: 'Username',
-                    label: 'asd',
-                  },
-                ]}
+                items={data?.map((invitation) => {
+                  return {
+                    name: invitation?.type === 'link'
+                      ? i18n['users.invite.list.link'][lang].replace('{link}', invitation?.link)
+                      : i18n['users.invite.list.user'][lang].replace('{name}', invitation?.invitee),
+                    controls: ['delete'],
+                  }
+                })}
               />
             )}
             {/* <UserTag showAvatar={true} size='l' user={user} /> */}
