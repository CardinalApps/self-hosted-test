diff a/apps/admin-web/src/pages/Users/UserManagementDrawer.tsx b/apps/admin-web/src/pages/Users/UserManagementDrawer.tsx	(rejected hunks)
@@ -1,4 +1,5 @@
 import { useSelector } from 'react-redux'
+import { ACi18n } from '@cardinalapps/access-control/src'
 
 import Alert from '@cardinalapps/ui/src/components/interaction/Alert'
 import Button from '@cardinalapps/ui/src/components/interaction/Button'
@@ -109,6 +110,16 @@ function UserManagementDrawer({ user, onClose }: UserManagementDrawerProps) {
           ]}
         />
       </Drawer.Section>
+      <Drawer.Section title={i18n['users.settings.roles'][lang]}>
+        <List
+          maxHeight={220}
+          items={user?.roles?.map((roleAssignment) => {
+            return {
+              name: ACi18n?.[`role.${roleAssignment.role}.name`]?.[lang],
+            }
+          })}
+        />
+      </Drawer.Section>
       <Drawer.Section className="user-actions" title={i18n['users.settings.actions'][lang]}>
         <EnableDisable user={user} />
         {removeUserButton()}
