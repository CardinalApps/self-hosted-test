diff a/servers/media/src/utils/logging.ts b/servers/media/src/utils/logging.ts	(rejected hunks)
@@ -12,6 +12,7 @@ export enum LogModule {
   EVENTS = 'Events',
   INDEXING = 'Indexing',
   JOBS = 'Jobs',
+  TRANSCODING = 'Transcoding',
 }
 
 /**
@@ -34,6 +35,9 @@ export const log = (module: LogModule, level: LogLevel, message: string) => {
     case LogModule.JOBS:
       levelSetByEnvironment = envVar('JOBS_LOG_LEVEL', LogLevel.INFO)
       break
+    case LogModule.TRANSCODING:
+      levelSetByEnvironment = envVar('TRANSCODER_LOG_LEVEL', LogLevel.SILENT)
+      break
   }
 
   if (levelSetByEnvironment !== LogLevel.SILENT && level >= levelSetByEnvironment) {
