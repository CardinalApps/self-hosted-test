diff a/servers/media/src/modules/claim/claim.service.ts b/servers/media/src/modules/claim/claim.service.ts	(rejected hunks)
@@ -40,12 +40,13 @@ export class ClaimService {
    * unreachable.
    */
   async claimServerWithCloudIfNotClaimed(): Promise<void> {
-    // Server must be claimed
+    // Server must have an owner
     const owner = await this.userService.getServerOwner()
     if (!owner) {
       return
     }
 
+    // And it must not already be claimed
     const alreadyClaimed = await this.databaseService.getOption(OPTIONS.CLAIM_ID.name)
     if (alreadyClaimed) {
       return
@@ -54,11 +55,11 @@ export class ClaimService {
     const instanceId = await this.databaseService.getOption(OPTIONS.INSTANCE_ID.name)
     const serverName = await this.settingsService.get(CardinalApp.ADMIN, 'server_name')
 
-    authAPI<ClaimRes>('/self-hosted/claim', 'POST', {
+    authAPI<ClaimRes>('/user/claims', 'POST', {
       body: {
         // Cardinal Admin app ID
         ssoAppId: '0d55d632-3517-4b1e-920e-448d6b77b8bf',
-        name: serverName as string,
+        appNameSetByUser: serverName as string,
         instanceId: instanceId as string,
       },
       JWT: owner.ssoToken,
