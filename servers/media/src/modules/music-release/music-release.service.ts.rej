diff a/servers/media/src/modules/music-release/music-release.service.ts b/servers/media/src/modules/music-release/music-release.service.ts	(rejected hunks)
@@ -52,18 +52,24 @@ export class MusicReleaseService {
     const where = isNumeric(id)
       ? { id: id as number }
       : { musicReleaseId: id as string }
-    const musicRelease = await this.musicReleaseRepository.find({
-      where,
-      relations: {
-        ...relations,
-      },
-    })
 
-    if (!musicRelease.length) {
+    try {
+      const musicRelease = await this.musicReleaseRepository.find({
+        where,
+        relations: {
+          ...relations,
+        },
+      })
+
+      if (!musicRelease.length) {
+        return null
+      }
+
+      return musicRelease[0]
+    } catch (error) {
+      Logger.error(error)
       return null
     }
-
-    return musicRelease[0]
   }
 
   /**
