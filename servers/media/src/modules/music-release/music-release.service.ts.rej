diff a/servers/media/src/modules/music-release/music-release.service.ts b/servers/media/src/modules/music-release/music-release.service.ts	(rejected hunks)
@@ -120,25 +120,37 @@ export class MusicReleaseService {
   }
 
   /**
-   * Returns all releases according to the query.
+   * Returns new music releases.
    */
   async query(getMusicReleasesDto: GetMusicReleasesDto): Promise<[MusicRelease[], number]> {
-    const { take, skip, order, sort, artists, genres, tracks, thumbnails, metadata } = getMusicReleasesDto
-    return await this.musicReleaseRepository.findAndCount({
+    const {
       take,
       skip,
-      relations: {
-        artist: true,
-        artists: artists,
-        genres: genres,
-        tracks: tracks,
-        thumbnails: thumbnails,
-        metadata: metadata,
-      },
-      order: {
-        [sort]: order,
-      },
-    })
+      order,
+      sort,
+      artists,
+      genres,
+      tracks,
+      thumbnails,
+      metadata,
+      //libraries,
+    } = getMusicReleasesDto
+
+    const qb = this.musicReleaseRepository.createQueryBuilder('musicRelease')
+
+    if (artists) qb.leftJoinAndSelect('musicRelease.artist', 'artist')
+    if (artists) qb.leftJoinAndSelect('musicRelease.artists', 'artists')
+    if (genres) qb.leftJoinAndSelect('musicRelease.genres', 'genres')
+    if (tracks) qb.leftJoinAndSelect('musicRelease.tracks', 'tracks')
+    if (thumbnails) qb.leftJoinAndSelect('musicRelease.thumbnails', 'thumbnails')
+    if (metadata) qb.leftJoinAndSelect('musicRelease.metadata', 'metadata')
+
+    qb.orderBy(`musicRelease.${sort}`, order)
+      .take(take)
+      .skip(skip)
+
+    const r = await qb.getManyAndCount()
+    return r
   }
 
   /**
