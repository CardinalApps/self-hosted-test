diff a/servers/media/src/modules/job/job.service.ts b/servers/media/src/modules/job/job.service.ts	(rejected hunks)
@@ -110,7 +110,7 @@ export class JobService {
   async updateJob(id: number, data: Partial<Job>): Promise<Job | false> {
     const updated = await this.jobRepository.update({ id }, data)
 
-    // FIXME move to a new endpoint called POST /event
+    // FIXME move to a new endpoint
     if (updated.affected) {
       if (data.status === JobStatus.PAUSED) {
         const job = await this.getJob(id)
