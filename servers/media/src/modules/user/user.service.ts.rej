diff a/servers/media/src/modules/user/user.service.ts b/servers/media/src/modules/user/user.service.ts	(rejected hunks)
@@ -359,6 +373,19 @@ export class UserService {
     return guestAccount.enabled
   }
 
+  /**
+   * Checks if the given password is correct.
+   */
+  async verifyPassword(username: string, password: string): Promise<boolean> {
+    const user = await this.userRepository
+      .createQueryBuilder('user')
+      .addSelect('user.password')
+      .where('user.username = :username', { username })
+      .getOne()
+
+    return !!user
+  }
+
   /**
    * Returns all users according to the query.
    */
