diff a/servers/media/src/modules/playback-queue/playback-queue-item.service.ts b/servers/media/src/modules/playback-queue/playback-queue-item.service.ts	(rejected hunks)
@@ -26,16 +26,19 @@ export class QueueItemService {
    * Returns all queue items according to the query. Items are returned in order
    * of their position in the queue.
    */
-  async query(id: string, queryPlaybackQueuesDto: QueryPlaybackQueueItemsDto): Promise<[PlaybackQueueItem[], number]> {
-    const { take, skip } = queryPlaybackQueuesDto
+  async query(id: string, queryPlaybackQueueItemsDto: QueryPlaybackQueueItemsDto): Promise<[PlaybackQueueItem[], number]> {
+    const {
+      take,
+      position,
+    } = queryPlaybackQueueItemsDto
 
     return await this.queueItemRepository.findAndCount({
       take,
-      skip,
       where: {
         queue: {
           queueId: id,
         },
+        ...(position ? { position } : {}),
       },
       order: {
         position: 'asc',
