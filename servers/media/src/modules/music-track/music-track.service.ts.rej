diff a/servers/media/src/modules/music-track/music-track.service.ts b/servers/media/src/modules/music-track/music-track.service.ts	(rejected hunks)
@@ -103,6 +103,7 @@ export class MusicTrackService {
     }
 
     // playCount requires manual mapping
+    const count = await qb.getCount()
     const withRaw = await qb.getRawAndEntities()
     const result = withRaw.entities.map((track, index) => {
       return {
@@ -111,7 +112,7 @@ export class MusicTrackService {
       }
     })
 
-    return [result, withRaw.entities.length]
+    return [result, count]
   }
 
   /**
