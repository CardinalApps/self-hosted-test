diff a/servers/media/src/modules/music-track/music-track.service.ts b/servers/media/src/modules/music-track/music-track.service.ts	(rejected hunks)
@@ -87,4 +87,22 @@ export class MusicTrackService {
       return await this.musicTrackRepository.save(initial)
     }
   }
+
+  /**
+   * Returns recently added music releases.
+   */
+  // async getRecentlyAddedReleases(): Promise<[, number]> {
+  //   return await this.musicTrackRepository.find({
+  //     take,
+  //     skip,
+  //     relations: {
+  //       release: release ? { thumbnails: true } : false,
+  //       artists: artists,
+  //       metadata: metadata,
+  //     },
+  //     order: {
+  //       [sort]: order,
+  //     },
+  //   })
+  // }
 }
