diff a/servers/media/src/modules/music-track/music-track.service.ts b/servers/media/src/modules/music-track/music-track.service.ts	(rejected hunks)
@@ -58,7 +58,7 @@ export class MusicTrackService {
    * Returns all tracks according to the query.
    */
   async query(getMusicTracksDto: GetMusicTracksDto): Promise<[MusicTrack[], number]> {
-    const { take, skip, order, orderBy, metadata, release, artists } = getMusicTracksDto
+    const { take, skip, order, sort, metadata, release, artists } = getMusicTracksDto
     return await this.musicTrackRepository.findAndCount({
       take,
       skip,
@@ -68,7 +68,7 @@ export class MusicTrackService {
         metadata: metadata,
       },
       order: {
-        [orderBy]: order,
+        [sort]: order,
       },
     })
   }
