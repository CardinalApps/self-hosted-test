diff a/servers/media/src/modules/app/app.service.ts b/servers/media/src/modules/app/app.service.ts	(rejected hunks)
@@ -333,7 +333,9 @@ export class AppService {
       return null
     }
 
-    const updateIsAvailable = semver.lt(this.getHomeServerVersion(), latest.version)
+    const updateIsAvailable = semver.valid(latest.version) && semver.valid(this.getHomeServerVersion())
+      ? semver.lt(this.getHomeServerVersion(), latest.version)
+      : false
 
     age.latestVersion = latest.version
     age.latestVersionReleasedAt = latest.releasedAt
