diff a/servers/media/src/modules/app/app.service.ts b/servers/media/src/modules/app/app.service.ts	(rejected hunks)
@@ -145,8 +145,6 @@ export class AppService {
 
   /**
    * Handles the initial server setup.
-   * 
-   * FIXME: server gets marked as set up even if it fails
    */
   async initialSetup(setupData: ServerInitType): Promise<ServerInitResultType | false> {
     // All of these should be set to true by the end of this method
@@ -156,8 +154,8 @@ export class AppService {
       initialUser: false,
     }
 
-    let localUserJWT = null
-    let localUser = null
+    // The client will do another request later to log into this account
+    let accountToLogInto
 
     // The user can optionally log into a Cardinal account during first time
     // setup - if they do, create and link their home server account.
@@ -168,9 +166,8 @@ export class AppService {
         setupStageSuccess.initialUser = true
       } else {
         try {
-          const loginResponse = await this.authService.login(undefined, setupData.userCardinalJWT)
-          localUserJWT = loginResponse.JWT
-          localUser = loginResponse.user
+          const newServerOwner = await this.userService.createServerOwner(setupData.userCardinalJWT)
+          accountToLogInto = newServerOwner.userId
           setupStageSuccess.initialUser = true
         } catch (error) {
           Logger.error(error, 'FirstTimeSetup')
@@ -182,9 +179,7 @@ export class AppService {
     else {
       try {
         const guestAccount = await this.userService.getGuestAccount()
-        const loginResult = await this.authService.login(guestAccount.userId)
-        localUserJWT = loginResult.JWT
-        localUser = loginResult.user
+        accountToLogInto = guestAccount.userId
         setupStageSuccess.initialUser = true
       } catch (error) {
         setupStageSuccess.initialUser = false
@@ -217,8 +212,7 @@ export class AppService {
     if (Object.values(setupStageSuccess).every((result) => !!result)) {
       return {
         serverName: setupData.serverName,
-        localUserJWT,
-        localUser,
+        accountToLogInto,
       }
     } else {
       return false
