diff a/servers/media/src/modules/library/library.service.ts b/servers/media/src/modules/library/library.service.ts	(rejected hunks)
@@ -1,6 +1,6 @@
 import { BadRequestException, Injectable, Logger } from '@nestjs/common'
 import { InjectRepository, InjectDataSource } from '@nestjs/typeorm'
-import { Repository, DataSource } from 'typeorm'
+import { Repository, DataSource, In } from 'typeorm'
 
 import { Library } from './library.entity'
 
@@ -67,6 +67,20 @@ export class LibraryService {
     return library
   }
 
+  /**
+   * Query libraries.
+   */
+  async getLibraries(libraryIds: string[]): Promise<Library[]> {
+    return await this.libraryRepository.find({
+      where: {
+        libraryId: In(libraryIds),
+      },
+      relations: {
+        user: true,
+      },
+    })
+  }
+
   /**
    * Updates a library.
    */
