diff a/servers/media/src/modules/auth/auth.controller.ts b/servers/media/src/modules/auth/auth.controller.ts	(rejected hunks)
@@ -1,13 +1,12 @@
 import {
   Controller,
-  Body,
-  Post,
   Logger,
   ForbiddenException,
+  Post,
+  Body,
 } from '@nestjs/common'
 import {
   ApiTags,
-  ApiOperation,
   ApiUnauthorizedResponse,
 } from '@nestjs/swagger'
 
@@ -15,26 +14,40 @@ import { LoginDetails } from './dtos/LoginDetails.dto'
 import { AuthService } from './auth.service'
 
 import { LoginResponse } from './dtos/LoginResponse.dto'
+import { StandardEndpoint } from '../../decorators/StandardEndpoint.decorator'
+import { OriginApp } from '../../decorators/OriginApp'
+
+const loginEndpointDescription =
+`Logs the user into a local account.
+
+If logging into a local offline account, no credentials are required.
+
+If logging into a local online account, a valid Cardinal SSO token is required.`
+
+const loginEndpointUnauthorizedDescription = 'Returns a 401 if there is an issue with the SSO token.'
 
 @Controller()
+@ApiTags('Authentication')
 export class AuthController {
   constructor(
     private readonly authService: AuthService,
   ) {}
 
   /**
-   * Logs the user into a local account.
-   * 
-   * If logging into a local offline account, no credentials are required.
-   * 
-   * If logging into a local online account, a valid Cardinal SSO token is
-   * required.
+   * Handler for all app logins that have passed auth and RBAC.
    */
   @Post('/login')
-  @ApiTags('Authentication')
-  @ApiOperation({ summary: 'Log into a local account.' })
-  @ApiUnauthorizedResponse({ description: 'Returns an Unauthorized error if there is an issue with the Cardinal token.' })
-  async login(@Body() loginDetails: LoginDetails): Promise<LoginResponse> {
+  @StandardEndpoint({
+    auth: false,
+    manualCapabilities: ['AdminApp.Login'],
+    summary: 'Log into an application.',
+    description: loginEndpointDescription,
+  })
+  @ApiUnauthorizedResponse({ description: loginEndpointUnauthorizedDescription })
+  async login(
+    @Body() loginDetails: LoginDetails,
+    @OriginApp() originApp,
+  ): Promise<LoginResponse> {
     try {
       const loginResult  = await this.authService.login(loginDetails?.userId, loginDetails?.cardinalJWT)
 
