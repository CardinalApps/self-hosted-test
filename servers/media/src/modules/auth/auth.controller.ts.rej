diff a/servers/media/src/modules/auth/auth.controller.ts b/servers/media/src/modules/auth/auth.controller.ts	(rejected hunks)
@@ -61,6 +61,8 @@ export class LoginController {
     try {
       const loginResult  = await this.authService.login({
         localUserId: loginDetails?.userId,
+        localUsername: loginDetails?.username,
+        localPassword: loginDetails?.password,
         ssoJWT: loginDetails?.cardinalJWT,
         app: originApp,
       })
