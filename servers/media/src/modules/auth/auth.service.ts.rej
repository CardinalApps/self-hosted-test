diff a/servers/media/src/modules/auth/auth.service.ts b/servers/media/src/modules/auth/auth.service.ts	(rejected hunks)
@@ -9,6 +9,8 @@ import { CardinalApp } from '../../utils/apps'
 
 type LoginOptions = {
   localUserId?: string,
+  localUsername?: string,
+  localPassword?: string,
   ssoJWT?: string,
   app: CardinalApp,
 }
@@ -30,22 +32,36 @@ export class AuthService {
   async login(options: LoginOptions): Promise<LoginResponse> {
     const {
       localUserId,
+      localUsername,
+      localPassword,
       ssoJWT,
       app,
     } = options
 
-    if (!localUserId && !ssoJWT) {
-      throw new Error('Either a local user ID or a SSO token is required to log in')
-    }
-
-    // Logging in with only a local user ID
-    if (localUserId && !ssoJWT) {
-      return await this.localAuthStrategy.login(localUserId, app)
+    if (!localUserId && !ssoJWT && (!localUsername && !localPassword)) {
+      throw new Error('One of the following login methods is required: SSO token, local username and password, or the Guest Account ID')
     }
 
     // Logging in with a cloud account
     if (ssoJWT) {
       return await this.cardinalSSOStrategy.login(localUserId, ssoJWT, app)
     }
+
+    // Logging in with a local username and password
+    if (localUsername && localPassword) {
+      return await this.localAuthStrategy.login({
+        app,
+        localUsername,
+        localPassword,
+      })
+    }
+
+    // Logging in with only a local user ID
+    if (localUserId) {
+      return await this.localAuthStrategy.login({
+        app,
+        localUserId,
+      })
+    }
   }
 }
