diff a/servers/media/src/modules/rbac/rbac.service.ts b/servers/media/src/modules/rbac/rbac.service.ts	(rejected hunks)
@@ -16,20 +16,21 @@ export class RBACService {
   ) {}
 
   /**
-   * Gets all assignments for a single user.
+   * Gets all assignments for all or one user.
    */
-  async getAndCountAllRoleAssignments(): Promise<RoleAssignment[]> {
+  async getAndCountAllRoleAssignments(user?: User): Promise<RoleAssignment[]> {
     let found = null
 
     try {
       found = await this.roleAssignmentRepository.findAndCount({
+        ...(user ? { where: { user: { userId: user.userId } } } : {}),
         relations: {
           user: true,
         },
       })
     } catch (error) {
       Logger.error(error, 'RBAC')
-  }
+    }
 
     return found
   }
