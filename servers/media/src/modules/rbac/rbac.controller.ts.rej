diff a/servers/media/src/modules/rbac/rbac.controller.ts b/servers/media/src/modules/rbac/rbac.controller.ts	(rejected hunks)
@@ -43,8 +43,19 @@ export class RBACController {
   @UseGuards(AuthGuard)
   @ApiOperation({ summary: "Get all role assignments." })
   @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
-  async getRoleAssignments(): Promise<RoleAssignment[]> {
-    const assignments = await this.rbacService.getAndCountAllRoleAssignments()
+  async getRoleAssignments(@Query('userId') userId?: string): Promise<RoleAssignment[]> {
+    let assignments = []
+
+    if (userId) {
+      const user = await this.userService.getUserByLocalId(userId)
+      if (!user) {
+        throw new BadRequestException('Invalid user ID')
+      }
+      assignments = await this.rbacService.getAndCountAllRoleAssignments(user)
+    } else {
+      assignments = await this.rbacService.getAndCountAllRoleAssignments()
+    }
+
     return assignments
   }
 
