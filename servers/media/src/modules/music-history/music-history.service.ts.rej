diff a/servers/media/src/modules/music-history/music-history.service.ts b/servers/media/src/modules/music-history/music-history.service.ts	(rejected hunks)
@@ -26,8 +26,15 @@ export class MusicHistoryService {
    */
   async createPlaybackEntry(user: User, createMusicHistoryEntryDto: CreateMusicHistoryEntryDto): Promise<MusicHistory> {
     const track = await this.musicTrackService.get(createMusicHistoryEntryDto.trackId)
+    const duration = track?.duration || 0
+
+    let progress = createMusicHistoryEntryDto.seconds / duration
+    if (progress > 0.991) {
+      progress = 1
+    }
+
     const entry = await this.musicHistoryRepository.save({
-      progress: createMusicHistoryEntryDto.progress,
+      progress,
       user,
       track,
     })
@@ -47,7 +54,10 @@ export class MusicHistoryService {
         [sort]: order,
       },
       relations: {
-        track: true,
+        track: {
+          release: true,
+          artists: true,
+        },
         user: true,
       },
     })
